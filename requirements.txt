# 김대리(officeclaw) — Python 사이드카 의존성 (pip 폴백)
# 정본은 services/sidecar/pyproject.toml + uv.lock 이다. uv 가 있으면 이 파일을 쓰지 말고
#   cd services/sidecar && uv sync --extra dev
# 를 쓴다. 이 파일은 uv 를 못 쓰는 환경에서 scripts/setup.ps1·setup.sh 가 venv 에 붓는 용도다.
# **이걸 깔아도 앱 실행 준비가 끝나는 건 아니다** — Node·Rust·Ollama·모델은 setup 스크립트가 맡는다.
# 설치: pip install -r requirements.txt
# 동기화 권장: cd services/sidecar && uv sync --frozen
# OneDrive 경로 충돌 회피(Windows): $env:UV_PROJECT_ENVIRONMENT="$env:LOCALAPPDATA\officeclaw\venvs\python-sidecar"

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
keyring>=25.3.0
httpx>=0.27.0
pydantic>=2.9.0
python-multipart>=0.0.6
openpyxl>=3.1.0
pandas>=2.2.0
xlsxwriter>=3.2.0
python-docx>=1.1.0
reportlab>=4.2.0
websockets>=12.0
xlwings>=0.33.0
# OS별 Excel 자동화 런타임
pywin32>=306; platform_system == "Windows"
# 모노레포 공용 패키지 — 사이드카가 import 한다(2026-09-06 감사 A3)
-e ./packages/protocol/python
-e ./packages/py-shared
appscript; platform_system == "Darwin"

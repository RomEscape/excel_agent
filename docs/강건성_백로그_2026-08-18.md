# 강건성 백로그 — 5렌즈 코드 사냥 70건 (2026-08-18)

5개 관점(어휘·파괴 경로·멀티턴·복합문·도메인)의 병렬 코드 정찰로 수집한
취약 문형 후보. mechanism은 파일:규칙 수준 근거이며, 처리 시 실측 재현 →
수정 → 회귀 테스트 순서를 지킨다. 처리한 항목은 날짜와 커밋을 적고 지운다.


## 조용한 파괴 (최우선) — 37건

### A1:C10에서 서식은 그대로 두고 값만 지워줘
- 기대: clear_range(A1:C10)만 — 테두리·배경 유지
- 근거: excel_live.py:3105 리셋 분기의 서식 걷어내기 정규식(~3140, `(표|테이블|...|서식|...)`)이 '서식' **언급만** 보고 '그대로 두고/유지/남기고' 부정 문맥을 안 본다. explicit_range가 있어 subset 가드(3095)도 건너뛴다. apply_border(none)+fill(none)+clear 3단계가 나오는데, 다단계 계획은 underfits 검사도 면제(~7449)고 첫 액션 apply_border가 fast-path라 즉시 실행 — 지키라던 서식을 지운다.

### 마지막 행만 지워줘
- 기대: 마지막 한 행만 삭제 (플래너의 drop_row 경로)
- 근거: excel_live.py:1685 _CLEAR_ROW_TARGET = `(중복|빈\s*(?:행|줄|칸)|\d+\s*(?:번째\s*)?(?:행|줄))` — 숫자 없는 '마지막 행'/'두 번째 행'이 안 잡혀 _clear_request_targets_a_subset=False. 리셋 분기(3105)가 subset 가드를 통과해 clear_range(__ACTIVE_SELECTION__)를 내고, clear_range는 fast-path라 즉시 실행 — 선택 영역 전체가 비워진다. '2번째 행'이라고 쓰면 가드가 잡는 것과 대조적.

### 매출이 3만 5천 원 이상이면 노란색으로 칠해줘
- 기대: 임계값 35,000으로 조건부 강조
- 근거: korean_number.py:_CONDITION_PATTERN(~80)은 숫자+배수 하나만 보고 비교어까지 이어져야 하는데 '3만' 뒤에 '5천'이 끼어 3만에서 실패하고 '5천 원 이상'에서 매치 → 임계값 5,000 (35,000이 아님). 게다가 parse_text_equals_condition이 '이상이면'에서 가짜 토큰 '이상'을 첫 매치로 돌려줘(excel_live_agent.py ~336, _TEXT_EQUALS_SKIP에 '이상' 없음) excel_live.py ~7440의 text-equals fast-path가 켜진다 — 잘못된 임계값 5,000짜리 highlight가 플래너 없이 확정 실행돼 7배 넓은 범위가 칠해진다.

### F열 빨갛게 칠해줘
- 기대: F:F를 빨간색(#FF4D4F 계열)으로 채움
- 근거: 색 어휘가 기본형만 열거돼 ㅎ-불규칙 활용형이 전멸. routers/excel_live.py:1336 _QUICK_COLOR_PATTERN과 services/excel_live_agent.py:163 _COLOR_TOKEN 모두 '빨간색|빨강|빨간'만 있고 빨갛게/파랗게/노랗게/하얗게/까맣게가 없다. 퀵 fill 분기는 '칠해' 토큰으로 진입하고 colors가 비어 기본 '#FFFF00'을 쓴다. fill_range는 fast path(routers:7448 '단순 색 채우기…should_parse_with_llm=False')라 LLM 미호출. 실측: _build_quick_action_plan → fill_range F:F #FFFF00(노랑).

### 임시 시트 지워줘
- 기대: delete_sheet(임시) 또는 최소한 확인 질문
- 근거: 시트 삭제 동사 그룹 '(?:삭제|제거|없애)'(routers:2637 delete_sheet_match, agent:534 동일)에 '지워'가 없어 미스. 대신 _is_clear_reset_request(routers:1647)의 '(지우|지워|…)'가 잡아 clear_range __ACTIVE_SELECTION__을 낸다(실측 확인). clear_range는 고신뢰 목록(routers:7422)이라 LLM 미호출로 즉시 실행 — 시트는 남고 현재 선택 영역 내용이 지워진 뒤 성공 보고.

### 여기 폰트 14로 바꿔줘
- 기대: set_font size=14
- 근거: _FONT_SIZE_PATTERN(agent:155)이 '크기|사이즈|size' 낱말 또는 'pt|포인트' 접미를 요구해 '폰트 14로'는 미파싱. 퀵 폰트 분기(routers:2946)는 '(굵게|볼드|bold|글꼴|폰트)'의 '폰트'로 진입한 뒤 font_params가 비면 {"bold": True} 기본값을 넣는다. set_font는 고신뢰 목록이라 LLM 미호출. 실측: set_font {bold:True} — 크기 대신 굵게가 적용됨.

### 미납인 건 빨간색으로 칠해줘
- 기대: 값=='미납' 조건부 강조(highlight_by_condition)
- 근거: _TEXT_EQUALS_PATTERN(agent:318)이 '인 행|인 셀|인 애들|인 것들|인 데|인 곳'은 알지만 단수 구어 '인 거/인 건/인 것만'이 없다. 실측: parse_text_equals_condition('미납인 건…')=None, ('완료인 것만…')=None → 퀵 fill 분기가 조건 없이 fill_range __ACTIVE_SELECTION__ #FF4D4F. fill fast path라 LLM 미호출, _quick_plan_underfits_message도 False(조건 파서 전부 미검출)라 가드 불발 — 전체가 빨갛게 칠해짐.

### B3에다 500 넣어줘
- 기대: B3에 500 입력
- 근거: 단일 셀 쓰기 패턴(agent:857 single_write_patterns)이 처소격을 '에' 한 글자로만 받아('\s*에\s*') '에다/에다가'의 '다(가)'가 값 그룹으로 흘러든다. 실측: parse_command_rule_based → write_range B3 values_2d=[['다 500']]. 명시 셀 쓰기는 preferred_write(routers:7862)로 플래너 결과까지 덮으므로 플래너가 맞게 풀어도 규칙의 오염 값이 이긴다.

### A2:C2에다가 이름,나이,점수 입력해줘
- 기대: A2:C2에 [이름,나이,점수] 한 행 입력
- 근거: _ROW_WRITE_PATTERN(agent:1564)도 '\s*에\s*'만 허용해 '에다가'의 '다가'가 값에 붙는다. 실측: parse_explicit_row_write(strong_verb_only=True) → values_2d=[['다가 이름','나이','점수']]. 이 결과는 선점 로직(routers:7285 preempt_write)이 퀵 계획으로 갈아끼우고 explicit_write 고정까지 걸려 플래너가 뒤집지 못한다 — 첫 칸이 '다가 이름'으로 확정.

### C3에 100 채워줘
- 기대: C3에 100 입력(write_range)
- 근거: 쓰기 동사 목록의 내부 불일치: 행 쓰기 _ROW_WRITE_PATTERN(agent:1564)과 parse_rangeless_row_write(agent:1638)에는 '채워'가 있는데 단일 셀 single_write_patterns(agent:857)에는 없다. 대신 색 분기 트리거 '(칠해|강조|표시|highlight|색|채워|배경|바꿔)'(agent:746)의 '채워'가 선점. 실측: parse_command_rule_based → fill_range C3 #FFFF00 — 값 입력 대신 노란 칠. 퀵은 None이라 플래너 실패·타임아웃 폴백 시 이 계획이 실행된다('B2를 500으로 바꿔줘'도 같은 경로로 fill_range B2 노랑 실측).

### 상태가 품절인 것만 빨간색으로 칠해줘
- 기대: 상태 열에서 값이 '품절'인 셀만 빨강 강조 (highlight =="품절")
- 근거: excel_live_agent.py:318 _TEXT_EQUALS_PATTERN 접미가 '인 것들'(복수형)뿐이라 '인 것만'을 못 잡아 text_equals=None. 라우터 색 분기(게이트 '칠해'+'빨간색')가 조건 없는 fill_range로 확정되고 excel_live.py:7447 fast-path로 quick_rule:hit — 실측 quick=[fill_range __ACTIVE_SELECTION__ #FF4D4F], underfit=False(조건·rank·data_state 전부 미검출). 선택영역 전체가 빨강 통칠.

### 수익률이 마이너스인 종목 빨갛게 칠해줘
- 기대: 수익률 열 <0 인 종목만 빨강 강조
- 근거: 이중 실패. (1) '마이너스/음수'가 조건 어휘 어디에도 없음(korean_number._CONDITION_PATTERN은 숫자 필수, 라우터 3991의 '음수'는 validate 힌트 전용)이고 '인 종목'은 _TEXT_EQUALS 접미 밖 → 조건 소실. (2) '빨갛게'는 _QUICK_COLOR_PATTERN(excel_live.py:1336, 빨간색|빨강|빨간)에 안 걸려 기본 #FFFF00. 실측 quick=[fill_range __ACTIVE_SELECTION__ #FFFF00] fast-path hit — 빨강을 시켰는데 전체가 노랑 통칠.

### 매출 상위 10%만 초록색으로 칠해줘
- 기대: 행수 비례 상위 10% 강조 (예: 40행이면 4개)
- 근거: excel_rank_limit.py:22 _EXPLICIT_RANK=(상위|하위|top|bottom)\s*(\d{1,4})가 뒤의 '%'를 통째로 무시 — 실측 detect=RankLimit(count=10). underfit으로 플래너에 갔다가 excel_live.py:8441 재환산이 파일을 읽어 '상위 10개' 경계값을 박음 → 행이 몇이든 정확히 10개만 칠해지고 성공 보고.

### 매출 높은 다섯 개만 노란색으로 칠해줘
- 기대: 매출 상위 5개만 강조
- 근거: excel_rank_limit.py:22,24 두 패턴 모두 \d{1,4} 요구라 한글 수사(다섯/열/스무)를 못 읽음 — 실측 rank=None. rank 가드가 안 걸리니 라우터 색 분기 fill_range가 fast-path hit(실측 underfit=False) → '다섯 개만'이 소실되고 대상 전체 노랑 통칠.

### 지난주보다 급증한 센서값 빨간색으로 강조해줘
- 기대: 판단 불가 → 되묻기 또는 데이터 관측 경로
- 근거: excel_live.py:1476 _DATA_STATE_PATTERN이 이상치/튀는 값/비정상만 알고 급증/급감/치솟/폭증이 없음(grep 0건). 실측 quick=[fill_range __ACTIVE_SELECTION__ #FF4D4F], underfit=False → fast-path hit → 조건 없이 전체 빨강 통칠.

### 품절 임박한 상품만 노란색으로 표시해줘
- 기대: 임박 기준(예: 재고 임계) 되묻기 또는 조건 좁힘
- 근거: '임박' 어휘가 사이드카 전체에 없고(grep 0건) '~한 상품'은 _TEXT_EQUALS 접미 밖. 라우터 색 게이트는 '노란색' 토큰으로 발동 → 실측 quick=[fill_range #FFFF00] fast-path hit → '만' 한정이 소실되고 전체 노랑 통칠.

### 수익률 5% 미만인 종목 빨간색으로 칠해줘
- 기대: 비율(0.05) 기준으로 강조 — 시트가 0.x로 저장돼 있으면 threshold 0.05
- 근거: excel_live.py:1361이 parse_condition의 percent 플래그를 버리고(실측 quick threshold=5.0), 보정 담당 excel_param_binder.py:1348 _looks_like_ratio_column은 all(0<=v<=1)이라 마이너스 수익률 샘플이 하나라도 있으면(포트폴리오 데모의 기본 상황) 비율 열로 인정 안 해 5→0.05 나눗셈이 건너뜀 → 0.x 값 전부 <5 → 열 전체 빨강.

### 턴1: A10에 서울 입력해줘 || 턴2: 아니 B2로 옮겨줘
- 기대: A10의 값을 B2로 이동(또는 못 하면 되묻기)
- 근거: excel_correction_context.py: _NOT_A_VALUE_EDIT(취소|되돌|지워|삭제|없애|빼|비워)에 이동 동사 '옮겨/이동'이 없고, _VALUE_BEFORE_RO가 'B2로'에서 'B2'를 새 값으로 캡처한다. parse_correction은 값이 셀 주소 형태인지 검사하지 않는다 → write_range(A10, [['B2']]) 퀵 계획. excel_live.py:7893 explicit_write 블록(문장에 B2가 있어 RANGE_REF 충족)이 플래너 결과를 이 규칙 쓰기로 뒤집으므로 A10에 문자열 'B2'가 기록된다.

### 턴1: 매출 시트 A10에 서울 입력해줘 (사용자는 요약 시트를 보는 중) || 턴2: 아니 부산으로 바꿔줘
- 기대: 매출!A10이 부산으로 정정
- 근거: excel_live.py:8970 record_last_write는 sheet_name을 primary.params.get('sheet_name')에서만 읽는데, 규칙 쓰기 params는 {start_cell, values_2d}뿐(excel_live_agent.py:881,947) — 턴1은 resolve_sheet_from_message(7495)로 매출에 실행되지만 기억에는 sheet_name=''. build_correction_plan(excel_correction_context.py:238)은 last.sheet_name이 빈 값이면 sheet_name을 안 붙여 턴2가 활성 시트(요약)!A10에 부산을 쓴다. 매출!A10은 서울 그대로.

### 턴1: F2에 A2:A9 합계 수식 넣어줘 || 턴2: B10 아래 칸에는 개수 세는 수식 넣어줘
- 기대: B11에 =COUNTA(...) 계열 수식
- 근거: excel_correction_context.build_below_formula_plan은 _BELOW_MENTION('아래+칸')과 _AGG_WORDS('개수'→COUNTA)만 보고 문장의 명시적 셀 B10을 확인하는 가드(mentions_explicit_range류)가 전혀 없다. 대상은 무조건 직전 수식 셀+1행 → F3에 =COUNTA(A2:A9). set_formula는 excel_live.py:7437 고신뢰 퀵 목록이라 LLM 없이 즉시 실행되어 F3이 조용히 덮인다.

### 턴1: 정산 시트 거래내역 정렬해줘 → "어떤 열 기준으로 정렬할까요?" || 턴2: (사용자는 엑셀에서 원본 시트를 열어 둔 채) 금액 기준 내림차순으로
- 기대: 정산 시트가 정렬됨
- 근거: PendingCreateTableSlots에는 활성 시트 덮임을 막는 explicit_sheet_name(excel_live.py:282-285)이 있지만 PendingExcelOperationSlots(305)에는 같은 필드가 없다. _merge_operation_slots:4456 'slot.sheet_name = req.sheet_name or slot.sheet_name'이 답변 턴의 활성 시트(원본)로 덮고, sort 플랜(4720)에 sheet_name 파라미터가 없어 _execute_action이 req.sheet_name=원본에 정렬을 실행한다.

### 턴1: 거래내역 좀 정렬해줘 → "어떤 열 기준으로?" || 턴2: 합계 행 B31:F31은 건드리지 말고 금액 기준 내림차순으로 해줘
- 기대: 합계행을 제외한 표 전체 정렬
- 근거: _extract_operation_hints(excel_live.py:3863-3866)는 문장 속 아무 범위나 target_range/source_range에 넣는다 — '건드리지 말고' 같은 부정 문맥 구분이 없다. _merge_operation_slots:4457 slot.params.update가 그대로 병합하고 sort 플랜(4723)이 target_range=B31:F31로 실행 → 제외하라던 바로 그 구간만 정렬된다.

### 턴1: A1:D9에 표 테두리 그려줘 || 턴2: (엑셀에서 F2:H8을 새로 드래그하고) 내가 잡아놓은 데만 노란색으로 채워줘
- 기대: 새로 드래그한 F2:H8 채움
- 근거: excel_selection_context.py _DEICTIC은 '(잡은|칠한|지정한|블록 잡은|음영 친)+데/곳/범위'와 '드래그한|끌어 둔|끌어 놓은'만 알고 '잡아놓은/잡아둔' 활용형이 없다 → mentions_selection=False, 문장에 좌표도 없으니 decide_selection_source가 'context' 반환 → 프론트의 lastExcelRangeRef(직전 결과 A1:D9)가 대상이 되어 옛 표가 노랗게 칠해진다.

### 턴1: (A1:F3에 표 붙여넣기·정리로 context_range=A1:F3 형성) || 턴2: (H1 셀 하나를 클릭해 두고) 여기에 총합 써줘
- 기대: 클릭한 H1에 합계 기록
- 근거: excel_selection_context.resolve_context_range 마지막 가드 'if ":" not in live and ":" in ctx: return context_range' — 한 칸 선택을 전부 커서 잔상으로 간주해 옛 범위를 우선한다. '여기에'로 지금 선택을 콕 집은 진짜 한 칸 클릭(live=H1)도 이 가드에 걸려 대상이 낡은 A1:F3으로 남고, 쓰기가 붙여넣은 표 위로 향한다.

### 턴1: B3에 완료라고 입력해줘 || 턴2: 아니 빨간색으로 바꿔줘
- 기대: B3의 배경(또는 글자)색을 빨강으로
- 근거: parse_correction(excel_correction_context.py)에 색 낱말 제외 목록이 없다 — 마커 '아니'+_VALUE_BEFORE_RO가 '빨간색'을 새 값으로 캡처해 write_range(B3,[['빨간색']]) 퀵 계획을 만든다. 색칠 해석(fill_range)은 parse_command_rule_based 폴백에만 있는데 퀵(정정) 계획이 우선한다. write_range는 고신뢰 목록(7424) 밖이라 플래너가 돌긴 하지만, 타임아웃/오답이면 7787 'parsed is None and quick_action_plan' 경로로 셀 값이 문자열 '빨간색'으로 교체된다.

### 턴1: A10에 서울 입력해줘 || 턴2: 아니 부산이라니까
- 기대: A10이 '부산'으로 정정
- 근거: excel_correction_context._TRAILING_VERB(바꿔|변경|고쳐|수정|해|줘|주세요|해줘)에 종결 어미 '이야/이라고/(이)라니까'가 없다. '아니 부산이라니까'는 _VALUE_BEFORE_RO 불일치(로 없음) 후 폴백에서 꼬리를 못 벗겨 value='부산이라니까'(공백 없음, 40자 이하, 지시대명사 아님)로 통과 → A10에 조사째 '부산이라니까'가 쓰인다.

### A1:C10에서 중복된 행은 지워줘
- 기대: A1:C10 안의 중복 행만 제거(dedupe_rows) 또는 되묻기
- 근거: routers/excel_live.py _build_quick_action_plan clear분기(~3127): 부분삭제 가드가 `if not whole_sheet_reset and not explicit_range and _clear_request_targets_a_subset(...)`라서 명시 범위가 있으면 '중복/된 행' 신호가 무시된다. target=A1:C10으로 clear_range 단일 계획이 나오고, clear_range는 고신뢰 목록(~7418)이라 LLM 없이 실행 — 범위 전체 값이 삭제된다. 언더핏 가드(_DATA_STATE_PATTERN:1478)에도 '중복'은 없다.

### 표 서식만 지워줘, 값은 그대로 두고
- 기대: 테두리·배경만 걷어내고 셀 값은 보존
- 근거: routers/excel_live.py clear/reset분기(~3131-3190): '서식'이 서식류 정규식(표|테이블|서식|…)에 걸리면 apply_border(none)+fill(none) 뒤에 clear_range가 **무조건** append된다 — '만'(서식 한정)을 보는 코드가 없다. 3단계 계획이라 _quick_plan_underfits_message는 len<=1 조건(~7455)으로 건너뛰고, 첫 액션 apply_border가 고신뢰 목록이라 LLM 생략 → 선택 범위 값이 통째로 삭제된다.

### 걸어놓은 필터 전체 초기화해줘
- 기대: 자동 필터 해제만 (데이터 보존) 또는 되묻기
- 근거: routers/excel_live.py _is_clear_reset_request(1647): '초기화'가 clear 동사 교체군에 있고, _clear_request_targets_a_subset(1692)에 '필터' 어휘가 전혀 없다. '전체'로 _is_whole_sheet_reset_request(1709) True → target=__USED_RANGE__, '초기화'가 서식류 정규식에도 걸려 border+fill+clear_range 3단계 → 시트 사용범위 값·서식 전부 삭제. apply_border 고신뢰라 LLM 미호출.

### 합계 행만 지워줘
- 기대: 합계 라벨이 있는 마지막 행만 비우기 또는 되묻기
- 근거: routers/excel_live.py _CLEAR_ROW_TARGET(1696)이 `중복|빈 행|숫자+행`만 알아서 '합계 행/마지막 행'처럼 이름·위치로 부른 행은 부분삭제로 인식 못 함 → 가드 미발동. target=normalized_ctx(직전 턴 결과 범위) or __ACTIVE_SELECTION__ 이라 표 전체가 clear_range 대상이 된다. clear_range 고신뢰 → LLM 없이 실행, 성공 보고.

### B2:B4에 12,000, 8,500, 9,300 입력해줘
- 기대: B2=12000, B3=8500, B4=9300 (천 단위 콤마는 값의 일부)
- 근거: agent:parse_explicit_row_write(1580)+_split_header_tokens(1690)이 콤마를 무조건 구분자로 봐 토큰 6개를 만들고 col_count=1로 잘라 B2에 12만 쓴다. 이어 binder:_bind_write_values(733)가 existing 1<3이라 원문 재추출 → _shape_write_values(630)는 1×1일 때만 콤마를 보호하고 범위는 split(",") + row_count 클램프 없음 → [[12],[0],[8],[500],[9],[300]] 6행이 B2:B7에 써져 B5:B7까지 덮는다.

### A2:C2에 서울,부산,대구 넣고 D2에 합계 써줘
- 기대: A2:C2에 서울/부산/대구, D2에 합계(수식 또는 라벨) 두 절 모두 실행
- 근거: agent:_ROW_WRITE_PATTERN(1564) 동사 교체군(입력|기록|작성|적어|넣어|채워|써…)에 연결형 '넣고'가 없어 lazy 값 그룹이 뒤 절의 '써'까지 삼킨다 → 값='서울,부산,대구 넣고 D2에 합계' → C2에 '대구 넣고 D2에 합계'가 텍스트로 들어가고 D2는 미실행. 라우터 선점(7287)이 이 계획을 quick으로 고정하고, binder:_bind_write_values는 existing_cells 3>=3이라 조기 반환해 못 고친다.

### A2:C2에 서울, 부산, 대구 순서대로 입력해줘
- 기대: C2=대구 ('순서대로' 같은 꼬리 부사는 값에서 제외)
- 근거: agent:parse_explicit_row_write는 마지막 토큰의 명령 꼬리를 떼는 로직이 없다 — _extract_listed_headers(1730대)의 '이렇게…' 꼬리 제거는 표 생성 경로 전용. 콤마 분해 후 '대구 순서대로'가 한 토큰으로 남아 C2에 그대로 써진다. '차례대로/하나씩/각각'도 동일. 선점 경로(7287)로 quick 확정이라 플래너 교정 기회 없음.

### 아 D열은 지워줘 (직전 턴에 A1:F10 표 작업을 한 다음)
- 기대: D열(D:D)만 비우기
- 근거: 프론트 hasExplicitRangeInCommand(WorkspacePage.jsx:182)는 숫자 없는 'D열'을 못 잡아 lastExcelRangeRef(A1:F10)가 context_range로 내려오고, clear분기 target이 `normalized_ctx or explicit_range`(3131) 순서라 옛 범위가 'D:D'를 이긴다. clear_range는 _CONDITION_FORMAT_ACTIONS(param_binder:933)에 없어 _bind_condition_format의 열 좁히기 구제도 못 받는다 → A1:F10 전체 값 삭제. _CLEAR_COLUMN_TARGET(1694)도 한 글자 열('D')은 2자 이상 요구라 미인식.

### C10에 매출 합계 넣어줘
- 기대: C10에 =SUM(매출열구간) 수식 또는 되묻기
- 근거: binder:_COMPUTE_REQUEST(676)가 명사형 '합계/총합/평균'을 의도적으로 제외해 write_values_echo_the_request가 미발동 → build_aggregate_formula(=SUM 변환, 1666)도 호출되지 않는다. 플래너 실패 시 폴백인 agent:single_write_patterns(857)는 셀 지목형에 어휘 가드가 전혀 없어 C10에 텍스트 '매출 합계'를 써 기존 값을 덮고 성공 보고한다. _AGGREGATE_FUNCS(1650)에 합계가 있지만 에코 감지를 거쳐야만 쓰인다.

### 제목 셀이랑 그 밑에 칸 병합 좀 해줘
- 기대: 셀 병합(또는 미지원이면 되묻기) — 수식 쓰기는 아님
- 근거: excel_aggregate_below.py _FUNC_VOCAB SUM 항목(23-38) `합(?:을|이|만|과|값|\s+좀|이나)`에 좌측 경계가 없어 '병합 좀'의 '합 좀'에 매칭된다('통합을'도 동일 — 주석은 통합을 피했다고 하지만 조사 붙은 꼴은 뚫림). '밑'이 _BELOW에 걸려 match_aggregate_below가 SUM을 확정, 라우터(~7305)가 사용범위를 읽어 표 아랫줄에 =SUM 수식+합계 라벨을 써 버린다. 병합 퀵 규칙이 없어 이 경로가 선점.

### A1:B2에 상품,수량,사과,3,배,5 입력해줘
- 기대: 2×2 범위에 4칸만 채우거나 값 개수 불일치 되묻기 — A3행은 건드리지 않기
- 근거: agent:parse_explicit_row_write는 세미콜론 없으면 한 행으로 보고 tokens[:2]만 써 [상품,수량]으로 축소(뒷값 4개 무언 폐기). 이어 binder:_bind_write_values가 existing 2<4라 원문 재추출 → _shape_write_values(630) 격자 경로 `[parts[i:i+col_count]]`에 row_count 클램프가 없어 3행([배,5]가 A3:B3)을 만들어 명시 범위 밖 행을 덮어쓴다(start_cell도 top_left로 강제, 769).


## 오동작 — 22건

### 매출 높은 순으로 정렬하고 제목 줄은 굵게 해줘
- 기대: sort_range + set_font(1:1) 두 단계
- 근거: excel_live.py:_build_quick_action_plan에 정렬 분기가 아예 없다. 글꼴 분기(2942)가 '굵게'만 보고 set_font(1:1) 단일 계획을 return하고, set_font는 고신뢰 fast-path 목록(~7429)에 있어 should_parse_with_llm=False. 이때 '정렬' operation_hints는 ~7885의 '고신뢰 퀵이 확정한 턴' 블록에서 지워진다. set_font는 어떤 underfits 민감 목록(1408/1478/1489)에도 없어 미달 판정도 안 뜬다 — 정렬 절이 통째로 증발.

### A1:D10 노란색으로 칠하고 테두리도 쳐줘
- 기대: fill_range(노랑) + apply_border 두 단계
- 근거: excel_live.py:2885 테두리 분기가 색 채우기 분기(3013)보다 먼저 있고 apply_border 한 단계만 return한다. 6f82163에서 글꼴+배경 복합은 두 단계로 고쳤지만(2960-2975) 테두리+배경 복합은 같은 구조인데 안 고쳤다. apply_border는 fast-path 목록에 있어 플래너도 안 탄다 — 검정 테두리만 그려지고 노란 칠은 증발, 성공 보고.

### 제목 줄 굵게 하고 표에 테두리도 그려줘
- 기대: set_font(1:1 bold) + apply_border 두 단계
- 근거: excel_live.py:2885 테두리 분기가 먼저 return하고, 설령 순서를 바꿔도 글꼴 분기 진입 조건에 `not re.search(r"(테두리|경계선|border|괘선)")`(2947)가 있어 테두리가 언급된 문장에선 글꼴 분기가 원천 차단된다. 굵게 절이 양쪽에서 다 버려진다. apply_border fast-path → 플래너 미호출.

### 테두리는 빼고 배경만 노란색으로 칠해줘
- 기대: fill_range(노랑)만, 테두리 없음
- 근거: excel_live.py:_has_border_style_context(1553)는 '테두리' 낱말만 보고 부정을 안 본다. 2885 분기의 border_remove 목록(2899)은 [없애,제거,지워,remove]뿐이라 '빼고'가 없어 line_style='continuous'로 검정 테두리를 **긋는다**. 그리고 그대로 return하니 뒤 fill 분기(3013)의 노란 칠도 증발. 사용자가 하지 말라는 것을 하고 하라는 것은 안 하는 완전 역전.

### 빨간색 말고 노란색으로 칠해줘
- 기대: fill_range(#FFFF00)
- 근거: excel_live.py:_quick_extract_colors(1346)는 등장 순서대로 색을 모으고 fill 분기(3048)는 무조건 colors[0]을 쓴다. '말고/대신' 배제 처리가 코드 어디에도 없어 primary=#FF0000 — 사용자가 거부한 바로 그 색을 칠한다. fill_range는 ~7434 elif에서 fast-path라 플래너 검증도 없다.

### 굵게는 하지 말고 크기를 16으로 바꿔줘
- 기대: set_font(size=16)만, bold 없음
- 근거: excel_live_agent.py:extract_font_params(209)의 bold 판정 `(굵게|볼드|bold|굵은|두껍)`이 부정 elif보다 먼저 매치되고, 부정 어휘는 `(굵기\s*(?:해제|없|빼)|보통\s*굵기|not\s*bold)`(212)뿐이라 '하지 말고'를 못 읽는다. bold=True+size=16으로 set_font — 명시적으로 금지한 굵게가 적용된다. set_font fast-path.

### 완료면 초록색, 지연이면 빨간색으로 칠해줘
- 기대: 완료→초록, 지연→빨강 두 개의 조건부 강조
- 근거: excel_live_agent.py:parse_text_equals_condition(~336)은 finditer 루프에서 **첫 매치만** return한다('완료'). excel_live.py fill 분기(3078)는 그 값 하나로 highlight 한 단계만 만들고, else 분기 처리(3052)는 숫자 조건+'아니면'에만 있다. ~7440의 text-equals fast-path 규칙이 should_parse_with_llm=False로 못박아 플래너 보정 기회도 없다 — 지연→빨강 절이 증발.

### 상위 세 개만 노란색으로 칠해줘
- 기대: 상위 3개 셀만 강조 (또는 열을 못 정하면 되묻기)
- 근거: excel_rank_limit.py:_EXPLICIT_RANK(21) `(상위|하위|top|bottom)\s*(\d{1,4})`와 _COMPARATIVE_RANK 모두 아라비아 숫자만 받는다. '세 개'는 detect=None → excel_live.py:1489 _RANK_LIMIT 민감 검사가 안 걸려 fill_range가 대상 전체를 칠한다. '상위 3개'는 플래너로 가게 고쳐놨는데 한국어 수사만 쓰면 그 가드가 통째로 무력화된다.

### 매출 상위 3개만 굵게 표시해줘
- 기대: 상위 3개 행만 굵게 (되묻기 허용)
- 근거: excel_live.py:1489 _RANK_LIMIT_SENSITIVE_QUICK_ACTIONS = {fill,clear,border,write} — set_font가 빠져 있다. rank_limit.detect('상위 3')는 성공하지만 글꼴 분기(2942)가 '굵게'로 set_font(__ACTIVE_SELECTION__)를 내고 set_font는 fast-path(~7429)라 미달 판정 없이 실행 — 선택 영역 전체가 굵어진다. 같은 문장에 '노랗게'를 쓰면 fill이 rank 민감이라 플래너로 가는 것과 비대칭.

### 십만 이상은 전부 빨간색으로 칠해줘
- 기대: 100,000 이상 셀만 조건부 강조
- 근거: korean_number.py:_NUMBER_WITH_SCALE(23) `(-?\d[\d,]*...)`가 선행 아라비아 숫자를 요구해 순한글 '십만'은 parse 실패 → _quick_parse_condition=None. '이상'도 숫자 없인 어느 조건 파서에도 안 걸리고 _looks_like_column_comparison은 이/가 조사가 없어 미매치. fill 분기(3013)가 조건 없는 fill_range로 떨어지고 '전부'가 __USED_RANGE__를 켜서(1621) 시트 사용 영역 전체가 빨갛게 — fill fast-path(~7434)라 플래너 검증 없음.

### 아직 저장하지 마
- 기대: 저장하지 않음(noop) 또는 의도 확인
- 근거: 퀵 저장 분기(routers:3182)가 any(token in lowered for token in ["저장","save"]) 부분문자열 검사라 부정 활용형 '-지 마/-지 말고'를 못 가른다. save_workbook은 고신뢰 목록(routers:7422)이라 LLM 미호출. 실측: _build_quick_action_plan('아직 저장하지 마') → save_workbook. 하지 말라는 바로 그 행동을 즉시 실행.

### 여기에 서울,100; 부산,200 넣어놔
- 기대: 붙여넣기 문맥 범위에 2행 값 입력
- 근거: parse_rangeless_row_write(agent:1638)의 동사 검사 '(입력|기록|넣어|채워|써)\s*(?:해)?\s*(?:줘|주세요|줄래)?\s*[.!]?\s*$'가 끝 고정(anchored) 화이트리스트라 '놔/둬/주라/줘요' 및 '~', '!!', 이모지 꼬리를 전부 거부. 실측: '넣어놔'·'입력해줘요'·'입력해줘~' 모두 None → 2026-08-18에 고친 붙여넣기 쓰기 흐름이 이 활용형에서 도로 붕괴, 플래너/되묻기로 새어 값 나열이 통째로 위험해진다.

### B열 가운데 정렬해줘
- 기대: 셀 정렬(가운데 맞춤) — 최소한 데이터 정렬로 오인하지 않기
- 근거: _detect_operation_intent의 sort 키워드(routers:1929 '정렬' 등)가 부분문자열 매치라 '가운데 정렬'의 '정렬'을 물어 intent='sort'(실측 확인). 맞춤(alignment) 어휘는 두 파일 어디에도 없다(가운데/중앙/align grep 0건). sort 슬롯이 기준 열을 물은 뒤 사용자가 'B열'이라 답하면 행 순서가 실제로 뒤섞인다.

### 백업 시트 파줘
- 기대: create_sheet(백업)
- 근거: 같은 파일 안 두 규칙의 동사 목록이 어긋난다: named_sheet_match(routers:2568)는 '(?:만들|생성|추가|파\s*줘|파줘)'로 '파줘'를 알지만, '이라는/이름은' 없이 오는 일반형 create_sheet_match(routers:2593)와 agent:504는 '(?:만들|생성|추가|create|add)'뿐. 실측: 퀵·규칙 모두 None → 플래너로 새는데, 플래너의 시트 이름 훼손은 직전 커밋(fa7db0f)까지 잡던 실측 실패 유형이다.

### 재고 20 밑으로 떨어지면 경고색으로 표시해줘
- 기대: <20 강조 (경고색은 빨강/주황 확인 질문 허용)
- 근거: 라우터 색 게이트(excel_live.py:3013 부근 토큰 목록)에 '표시'·'경고색'이 없어 quick 미발동(실측 null) — _quick_parse_condition은 ('<',20)을 파싱하는데도 버려짐 → 플래너 룰렛. 플래너 실패 시 폴백은 excel_live_agent.py:377 op맵에 '밑'이 없어(korean_number엔 있음) _TEXT_EQUALS의 '떨어지'로 highlight =="떨어지" A:Z(실측) → 0건.

### 재주문점에 근접한 재고는 주황색으로 표시해줘
- 기대: 근접 기준(±얼마) 되묻기 또는 근접 조건 강조
- 근거: '근접'이 사이드카 전체 grep 0건 — 조건·비교·데이터상태 어느 레이어에도 없음. 라우터 색 게이트에 '표시'/'주황색'이 없어 quick=null(실측) → 플래너 룰렛. 실패 폴백은 excel_live_agent.py 조건 없는 채우기 기본 A:Z(실측 fill_range A:Z #ED7D31) → A:Z 통칠.

### 매출 상위 5개만 남기고 나머지는 지워줘
- 기대: 상위 5 제외 행 삭제, 또는 파괴적이니 되묻기
- 근거: 실측 quick=[clear_range __ACTIVE_SELECTION__] → rank-sensitive underfit=True로 플래너에 넘어가는 것까진 정상이나, excel_live.py:8441 rank 재환산은 _RANK_LIMIT_FORMAT_ACTIONS={fill_range,highlight_by_condition}(4222)만 교체 — 삭제 계획엔 개입 불가('남기고 지워'의 결정론 경로 부재). 플래너가 clear_range 전체를 내면 상위 5까지 그대로 지워지고 지운 셀이 있으니 성공 보고.

### 턴1: 지역별 매출 집계표 만들어줘 || 턴2: 원본데이터 시트 B2:B13으로 선 그래프 그려줘
- 기대: 원본데이터!B2:B13을 소스로 한 선 그래프
- 근거: 턴1 피벗 실행 시 excel_live.py:8731이 _last_aggregate_sheet[session_key]를 기록하는데 이 dict(393행)는 TTL도 pop도 없다. 턴2의 create_chart(고신뢰 퀵, 7424)는 _run_execute_once(8805)에서 _chain_chart_to_pivot(6990)을 지나며 source_sheet/output_sheet=집계 시트, source_range='__USED_RANGE__'로 무조건 덮인다 — 문장에 명시된 시트·범위를 존중하는 검사가 없다.

### 턴1: F2에 A2:A9 합계 수식 넣어줘 || 턴2: 아니 평균으로 바꿔줘
- 기대: F2 수식이 SUM→AVERAGE로 교체
- 근거: excel_correction_context에 수식 정정 경로가 없다: build_correction_plan은 LastWrite 전용(턴1은 set_formula라 record_last_write 미호출, excel_live.py:8969 조건), build_below_formula_plan은 '아래/밑+칸' 표지를 요구한다. LastFormula(F2,=SUM)가 기억돼 있는데도 아무 규칙도 못 쓰고 플래너로 넘어간다 — 모듈 첫머리 주석이 기록한 find/replace 역전(찾을말=평균, 바꿀말='')류 오답 경로 그대로. param_binder:1590 가드는 빈 치환만 막아 '합계→평균' 텍스트 치환이 나오면 시트의 다른 라벨이 바뀐다.

### 턴1: Sales_Data 시트 H1에 오늘 매출 써줘 → "Sales_Data 시트를 찾을 수 없습니다..." || 턴2: 됐고, 비고 열 이름을 메모로 바꿔줘
- 기대: 비고 열 머리글만 '메모'로 변경
- 근거: 턴1은 approval gate의 target_missing(excel_live.py:8615-8637)이 PendingClarification(원문+질문)을 저장한다. _drop_superseded_pending_slots(2210)는 슬롯만 다루고 clarification은 새 명령이 와도 안 버린다. 턴2(열 구조 편집 → _looks_like_column_structure_edit로 intent '' → 플래너 경로)의 파싱 컨텍스트에 conversation_history_text(7546)로 옛 요청 'Sales_Data H1에 매출 입력'이 통째로 주입돼, 플래너가 열 이름 변경에 옛 쓰기를 합치거나 옛 요청을 대신 실행할 수 있다. ask_count 승계 시 forbid_clarify(7547)가 실행을 강제한다.

### B열만 글씨 굵게 해줘 (직전 턴에 다른 범위를 작업한 다음)
- 기대: B열(B:B)만 굵게
- 근거: font 분기(routers:2953) target이 `normalized_ctx or explicit_range` 순서 — 프론트가 'B열'(숫자 없음)을 명시 범위로 인식 못 해(WorkspacePage.jsx:182) 옛 lastExcelRangeRef가 내려오고 col_range_ref 'B:B'를 이긴다. set_font는 고신뢰 목록이라 LLM 생략으로 확정 실행되고, _CONDITION_FORMAT_ACTIONS(933)에 없어 바인더의 열 좁히기 구제도 없다 → 직전 범위 전체가 굵게 된다. #8과 같은 뿌리(ctx-우선 순서)의 서식 버전.

### 매출 열은 퍼센트 형식으로 소수 둘째 자리까지 보여줘
- 기대: 표시 형식 0.00% (퍼센트+소수 두 자리 결합)
- 근거: routers:_NUMBER_FORMAT_HINTS(2454)는 첫 매칭 하나만 쓰고 결합이 없다 — 소수 규칙이 퍼센트보다 앞이라 '둘째 자리'가 먼저 걸려 format_code='0.00'이 된다. _quick_number_format_step은 빌더 두 번째 분기라 즉시 반환하고 set_number_format 실행 → 0.153이 '15.30%'가 아니라 '0.15'로 표시된다. 성공 보고만 나가 사용자는 비율이 100배 작게 보이는 표를 얻는다.


## 불필요한 되묻기 — 3건

### B2 값 알려줘
- 기대: read_range B2
- 근거: 읽기 동사 그룹 read_verbs(agent:688 '(읽어|읽기|보여|조회|확인|read|show|display)')와 퀵 조회 토큰(routers:2838 ["읽어","보여","확인","조회",…]) 모두에 '알려'가 없다. 실측: 규칙·퀵 전부 None → 가장 흔한 조회 말투가 매번 플래너 복불복으로 넘어간다(로컬 플래너는 같은 문장에 같은 계획을 보장 못함).

### 세 줄 네 칸짜리 표 만들어줘
- 기대: 3행 4열 create_table
- 근거: 표 크기 파서가 전부 아라비아 숫자만 받는다: _TABLE_AXIS_PATTERN(agent:1489 '(?P<count_first>\d{1,3})…'), 크기 매치(agent extract_create_table_slot_hints의 r'(\d{1,3})\s*행\s*(\d{1,3})\s*열'). 고유어 수사 한/두/세/네/다섯이 없다 — 소수점 서식(routers:2458)은 '네|4'를 받는 것과 대조적. 실측: rows=None cols=None table_intent=True → 크기를 이미 말했는데 크기 질문이 또 나간다.

### 턴1: 표 하나 만들어줘 → "표 크기와 헤더를 알려주세요" || 턴2: 잠깐, 지금 시트 하나 복사부터 해줘
- 기대: 시트 복사 실행(또는 미지원 안내)
- 근거: '시트 복사'는 규칙·intent 어디에도 없다(_build_quick_action_plan의 시트 규칙은 만들/생성/추가/이동/삭제뿐, '복사' grep 결과 규칙 없음) → _incoming_command_intent='' → _drop_superseded_pending_slots(2225)가 표 슬롯 유지 → 복사 요청이 크기 답변으로 병합되고 rows/cols 여전히 None → 같은 크기 질문 반복(8163, ask_count 2). 한 턴 더 지나면 _apply_table_size_fallback(5148)이 사용자가 확정한 적 없는 5x5 표를 강제 생성한다.


## 무동작 — 8건

### 클레임 비중 도넛으로 그려주고 저장해줘
- 기대: create_chart(doughnut) + save_workbook
- 근거: excel_live.py:3182 저장 분기가 '저장' 토큰 하나로 save_workbook 단일 계획을 return한다(_build_quick_action_plan의 마지막 분기지만 차트 분기는 이 함수에 없다). 라우터의 차트 선점 블록(~7391)은 `if not quick_action_plan:` 가드라 save 계획이 있으면 실행조차 안 된다. save_workbook은 fast-path(~7429) — 파일만 저장되고 차트는 안 생기는데 성공 보고.

### 머리글 진하게 해줘
- 기대: 1행 set_font bold=True
- 근거: 굵게 어휘가 '(굵게|볼드|bold|굵은|두껍)'(agent:207 extract_font_params)와 퀵 트리거 '(굵게|볼드|bold|글꼴|폰트)'(routers:2946)뿐 — 한국어에서 굵게의 가장 흔한 표현 '진하게/진한'이 없다. 실측: extract_font_params={} → 규칙·퀵 모두 None. '머리글+해줘'는 _EDIT_VERB(routers:3323)에도 안 걸려 _looks_like_excel_request가 미스, 업무 외 분류로 샐 수도 있다.

### 재고 30박스 미만이면 노란색으로 칠해줘
- 기대: 재고 열 <30 강조
- 근거: korean_number.py:80 단위 화이트리스트 (원|개|건|점|명|kg|일)에 '박스'가 없어 '30박스 미만'이 조건으로 안 붙고, _TEXT_EQUALS_PATTERN이 '미만이'+'면'을 값 동등 조건으로 오인. 실측 quick=[highlight == value="미만이"], excel_live.py:7453 text_equals 락으로 quick_rule:hit 확정 → '미만이'와 같은 셀 0건 강조 후 성공 보고. 톤/봉/팩/잔도 동일.

### 온도가 15보다 낮으면 파란색으로 칠해줘
- 기대: 온도 열 <15 파랑 강조
- 근거: excel_live.py:1364 than_match가 (크거나같|작거나같|큰|작은|크|작)만 알고 낮/높/많/적이 없음 — 열간 비교 패턴(1386)과 바인더 _COMPARISON_WORDS에는 '보다 낮'이 있는데 숫자 임계값 경로에만 빠짐. korean_number에도 '보다' 꼴 없음 → 조건 실패, _TEXT_EQUALS가 '낮으'+'면' 포획. 실측 quick=[highlight == value="낮으"] → 7453 락 hit → 0건. '80보다 높으면/100보다 많으면'도 동일.

### 습도 90 넘어가면 파란색으로 칠해줘
- 기대: 습도 열 >90 강조
- 근거: korean_number.py _CONDITION_WORDS에 '넘는/넘으면'은 있는데 '넘어가면/넘어가는/넘기면'이 없음. _TEXT_EQUALS가 '넘어가'+'면'을 값으로 포획 → 실측 quick=[highlight == value="넘어가"] → excel_live.py:7453 락으로 hit → 0건 강조가 성공으로 보고.

### 습도 60에서 80 사이 벗어나면 빨간색으로 강조해줘
- 기대: 60~80 구간 밖 강조(2조건) 또는 되묻기
- 근거: 구간 표현(에서~사이/벗어나)이 조건 파서 전체에 없음 — korean_number 조사 목록에 '에서' 없고 '사이/벗어나' 미등록. _TEXT_EQUALS가 '벗어나'+'면' 포획 → 실측 quick=[highlight == value="벗어나"] → excel_live.py:7453 락 hit → 0건 강조 성공 보고.

### 턴1: 차트 하나 그려줘 → "차트 종류를 선택해 주세요" || 턴2: 아 그냥 매출 대시보드 만들어줘
- 기대: 매크로 분해(대시보드 다단계 계획) 시작
- 근거: excel_live.py:7136-7147 매크로 분기는 pending_slot/pending_operation이 None일 때만 탄다. '대시보드 만들어줘'는 looks_like_macro_request(excel_macro_planner.py:120, _COMPOSITE_ARTIFACT '대시보드'+_SCALE_VERB '만들어')를 통과할 문장이지만 차트 슬롯 때문에 호출 자체가 안 된다. _detect_operation_intent에 '대시보드'는 갱신 결합 패턴(recalc)뿐이라 incoming='' → _drop_superseded(2225)가 슬롯 유지 → 답으로 소비되고 chart_type 재질문이 last_question과 같아 슬롯 폐기(8250) 후 계획 없음 → 일반 되묻기/400.

### 표가 좀 밋밋한데 가능하면 연한 노란색으로 칠해줘
- 기대: 선택 범위(또는 표)에 노란 배경 채우기
- 근거: agent:_TEXT_EQUALS_PATTERN(316)의 접미 '면'이 양보 표현 '가능하면'을 조건으로 오인해 token '가능하'를 만든다(_TEXT_EQUALS_SKIP에 없음). 라우터 색 분기(3046)에서 text_equals가 있으면 fill_range 대신 highlight_by_condition(=='가능하')이 되고, 7448의 `highlight+text_equals → should_parse_with_llm=False`로 확정 실행 → 일치 셀 0개, 아무것도 안 칠하고 성공 보고. '필요하면/괜찮으면'도 동일.

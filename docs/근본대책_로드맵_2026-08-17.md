<!-- 2026-08-17 웹 리서치 종합(5각도 병렬 조사). 출처는 본문 괄호 안 arxiv/공식 문서. -->

{
  "summary": "로컬 LLM 엑셀 에이전트의 근본 해결책 — 제품·논문·런타임 기법 병렬 조사",
  "agentCount": 6,
  "logs": [],
  "result": {
    "synthesis": "# 근본 해결책 로드맵 — 조사 5각도 종합\n\n전제: 상징 의도 JSON + 범용 모델(96~100%), 결정적 바인더, 154건 평가 배터리는 **이미 구현·실측 완료**이므로 아래에서 다시 제안하지 않는다. 이 로드맵은 남은 근본 한계 (a) 없는 액션 발명·형식 붕괴, (b) 7B의 복잡 요청 이해, (c) 한국어만 다룬다. 모든 항목은 §2 절차(before/after 실측 + 실행 id + 개발일지)로 게이트한다.\n\n핵심 확인: Microsoft Semantic Interpreter(ODSL+결정적 트랜스파일러, arxiv 2306.03460), SheetCopilot(액션 API가 자유 코드 생성을 Pass@1 2.7배로 이김, arxiv 2305.19308), TableGPT2(표 특화 7B는 236만 튜플이 필요했음, arxiv 2411.02059)가 모두 현 구조(제한 어휘 + 결정적 조립)가 업계·학계 수렴점임을 뒷받침한다. **방향 전환은 불필요하고, 아래는 그 구조의 빈 조각을 채우는 일이다.**\n\n---\n\n## 0. 하지 않을 것 — 자원 회수 (즉시 결정)\n\n| 중단/금지 | 근거 | 회수 효과 |\n|---|---|---|\n| 플래너(ax7bplanner) SFT 추가 투자 중단 | TableGPT2: 표 특화 7B 성립에 236만 튜플 필요(arxiv 2411.02059). 1000건 SFT의 파라미터 암기(58~67%)는 우리만의 실패가 아니라 규모의 문제 | 학습 데이터 제작·재학습 리소스를 아래 1~2절로 전환 |\n| 개방형 스프레드시트 에이전시 시도 금지 | SpreadsheetBench: 프런티어급도 57.2%(MS Agent Mode)~70.5%(Gemini), 인간 71.3%. 실사용 포럼 질의는 GPT-4o도 ~20%(arxiv 2406.14991) | 7B로 정면 돌파 불가능한 영역에 시간을 쓰지 않음 |\n| 상징 의도 JSON에 셀 좌표·범위 문자열 필드 추가 금지 | SUC: GPT-3.5도 셀 주소 조회 39.7~44%(arxiv 2305.13062). 좌표는 excel_param_binder 소관이라는 현 분업이 문헌으로 검증된 설계 | 리뷰에서 거부할 명시 기준 확보 |\n| 스키마에 pattern·oneOf 등 복잡 키워드 금지 | 엔진 간 커버리지 편차 최대 2배(JSONSchemaBench, arxiv 2501.10868) + 로컬 실측에서 pattern이 사용자 지시를 조용히 덮어씀 | 조용한 오답 유입 차단 |\n| FC 특화 SFT 소형 모델(ToolACE/xLAM/Hammer) 도입 안 함 | ax7bplanner에서 이미 반증한 '좁은 SFT 암기' 계열 + 한국어 미검증 | 모델 평가 비용 절약 |\n\n---\n\n## 1. 지금 당장 — 코드 몇 줄\n\n### 1-1. 구조화 출력(response_format json_schema)을 상징 의도 경로에 적용\n- **무엇**: `ollama_service.py:53`의 `chat()`에 `json_schema` 파라미터를 추가하고, 주어지면 `json_object` 대신 `response_format={\"type\":\"json_schema\", ...}`를 전송. 첫 적용 지점은 `excel_intent_normalizer.py:116`(96~100% 경로), 스키마 원천은 `excel_tool_schemas.py`의 `_clean_schema`(288행) 재사용. **task 필드를 구현된 액션만 담은 enum으로 선언.** 플래너(ax7bplanner) 경로에는 적용하지 않는다(`build_planner_prompt`는 SFT 생성과 같은 함수 — §3.5).\n- **근거**: Ollama 공식 지원(ollama.com/blog/structured-outputs), **이 머신·이 모델(ax4-light)·이 엔드포인트에서 강제 동작 로컬 실측 완료**(Ollama 0.32.14). 원리는 스키마→GBNF→로짓 마스킹이므로 어휘 밖 액션은 토큰 수준에서 생성 불가.\n- **예상 효과**: **형식 붕괴·없는 액션 발명(한계 a)은 구조적으로 0이 된다 — 이것이 보장 대상.** 정확도 변화는 별개다: XGrammar-2(arxiv 2601.04426)는 3B에서 33→78%였지만 그건 낮은 기준선에서의 이득이고, 우리는 이미 96~100%라 상승 폭은 작을 수 있다. 반대로 Constraint Tax(arxiv 2605.26128)는 초소형 모델에서 '유효-but-오답' 급증을 실측했으므로 **'스키마 유효하나 내용 오답' 비율을 새 지표로 추가**해 before/after를 잰다.\n- **함께 지킬 것** (출처가 명시한 조건):\n  - 프롬프트의 스키마 설명·액션 한국어 한 줄 설명은 유지한다 — 모델은 문법의 존재를 모르며, 로컬 실측에서 불투명 enum을 주자 sum 요청에 정렬 액션을 골랐다(제약은 형식만 보장, 의미는 프롬프트 몫).\n  - `finish_reason != \"stop\"`이면 절단 JSON으로 실패 처리 + max_tokens 여유 + temperature 0(Ollama 공식 권고).\n  - 기존 사후 파싱·validator·바인더는 제거하지 않는다 — 스키마는 열 존재 같은 시트 정합성을 못 본다.\n- **측정**: `run_command_diagnostics.py -n 3 --label before/after-structured-output`, '형식 붕괴 재시도 횟수'와 '발명 액션 발생률' 전후 비교.\n\n### 1-2. 다이제스트 예시행을 KV형으로\n- **무엇**: `excel_workbook_digest.py:254`의 예시행이 값만 위치 순서로 나열됨(`예시행: 서울 | 100`). `지역=서울 | 매출=100`처럼 머리글=값 쌍으로 변경.\n- **근거**: 소형 모델 11개 포맷 비교에서 Markdown-KV가 60.7%로 1위, 위치 기반 포맷 대비 +16%p(blog.iptek.web.id 실측). 단 이 수치는 GPT-4.1-nano 기준이고 포맷 승자는 모델마다 다르므로(TabVerse, arxiv 2606.09578) **채택은 자체 측정 결과로만 결정**.\n- **예상 효과**: 소형 모델의 열-값 대응 오인 감소 가능성 — 자체 전후 측정으로 확인, 이득 없으면 되돌린다.\n\n### 1-3. 서식 확장 금지 원칙 명문화\n- **무엇**: `excel_workbook_digest.py` 모듈 주석에 \"셀 서식·NFS를 다이제스트에 추가하지 않는다(활성 시트 layout만)\"를 출처와 함께 기록.\n- **근거**: SheetCompressor 실측 — 서식 포함 시 압축은 좋아지나 F1이 0.789→0.759로 하락(arxiv 2407.09025).\n- **예상 효과**: 코드 변경이 아니라 재발 방지. 측정 불필요.\n\n---\n\n## 2. 단기 — 1~2일\n\n### 2-1. 검증 실패를 재계획 루프에 되먹이기 (한계 b의 최대 지렛대)\n- **무엇**: 현재 `excel_step_repair`는 단발 보정이다. `excel_result_verifier`의 실패 내용(오류 메시지·사후조건 불일치 **원문**)이 다음 모델 호출 프롬프트에 그대로 들어가는 폐루프로 확장: 관측 → 원자 액션 1개 → 실행 → 검증 → 실패 시 오류 텍스트 + **갱신된** 다이제스트를 함께 회신. 이를 위해 다중 스텝 경로에서 쓰기 액션 성공 직후 다이제스트 캐시 무효화가 항상 일어나는지 감사한다.\n- **근거**: SheetCopilot ablation(arxiv 2305.19308) — 오류 피드백 단독이 Exec@1 92.8%로 최대 단일 기여 요인, 시트 상태 피드백만으로 Exec@1 +19%p, 결합 시 Pass@1 18.6→44.3%. (GPT-3.5 벤치마크 수치이므로 우리 절대치는 다르겠지만, '피드백 루프가 프롬프트 문구 개선보다 크다'는 서열은 방향 근거로 유효.)\n- **예상 효과**: 복잡·다단계 요청의 실행 성공률 개선 — 폭은 자체 측정으로.\n\n### 2-2. 바인더 단계에서 기대 사후조건을 미리 산출해 verifier에 전달\n- **무엇**: 태스크 유형별 기대 결과(예: 정렬 후 첫 행 값, 수식 열의 채워진 범위)를 바인더가 실행 **전에** 산출해 `excel_result_verifier`에 넘긴다. 검증이 \"뭔가 달라졌나\"가 아니라 결정적 대조가 된다.\n- **근거**: Microsoft Agent Mode의 '실행 전 경량 테스트 생성 → 단계별 검증 가드레일' 패턴(microsoft.com 2025-09 발표). 바인더가 이미 실제 좌표를 확정하므로 추가 모델 비용 없음.\n- **예상 효과**: 검증의 결정성 향상(정성적 — Microsoft는 이 요소 단독 수치를 공개하지 않았으므로 수치 주장 안 함). 2-1의 되먹임 품질을 올리는 기반.\n\n### 2-3. CONFIRM 등급에 영향 범위 프리뷰 → 승인 → 실행\n- **무엇**: 바인더가 확정한 대상 범위·작업 요약을 실행 전에 WorkspacePage 카드로 표시, 승인 후 실행, 실행 후 undo 제공.\n- **근거**: Google Sheets Gemini의 액션 프리뷰 카드(workspaceupdates 2025-05), Excel Copilot의 보라색 하이라이트 프리뷰-승인 흐름(support.microsoft.com). 프론티어 모델을 쓰는 두 회사 모두 이것을 신뢰 확보 수단으로 출시.\n- **예상 효과**: 모델 비용 0으로 체감 신뢰 확보(제품 관행 근거이며 정확도 수치 주장은 하지 않음). 오작동의 피해를 실행 전 차단.\n\n### 2-4. 어휘 밖 요청의 명시적 되묻기·거절 경로 + 평가축 추가\n- **무엇**: 스키마 enum 밖 의도가 감지되면 자유 생성으로 확장하지 말고 명확화 질문 또는 '지원 안 함' 응답으로 라우팅(tool_registry.py 권한 체계와 같은 층). 평가에는 Slot(누락 파라미터 되묻기)·Relevance(도구로 못 푸는 요청 거절) 두 축을 추가 — kakao/FunctionChat-Bench가 OpenAI 호환 엔드포인트를 지원하므로 그대로 돌리거나 자체 154건에 두 축 케이스를 증설.\n- **근거**: Google도 닫힌 5종 액션 + 프리뷰로 출시(workspaceupdates 2025-05). FunctionChat-Bench(github.com/kakao, arxiv 2411.14054)는 한국어 도구 호출 전용으로 Slot/Relevance를 채점 — excel_param_binder의 파라미터 확정 실패·거절 처리와 같은 문제를 측정. 한계 (c)의 평가 공백을 메운다.\n- **예상 효과**: 못 다루는 요청이 조용한 오답 대신 되묻기로 떨어짐. 액션 enum을 1개 늘릴 때마다 154건 + 새 두 축으로 게이트.\n\n### 2-5. 값→범위 역색인 실험 (조건부)\n- **무엇**: 다이제스트의 값 후보(categories)를 저카디널리티 열에 한해 `완료=B2:B14,B20` 식 역색인으로 확장. 필터/조건부 명령 케이스만 골라 전후 측정, **이득 없으면 버린다**.\n- **근거**: SpreadsheetLLM 역색인(arxiv 2407.09025). 단 같은 논문에서 서식 집계는 압축을 올리고 정확도를 깎았으므로 무조건 채택 금지.\n- **예상 효과**: 필터 파라미터 확정 정확도 개선 가능성 — 가설 단계, 측정으로만 채택.\n\n---\n\n## 3. 중기 — 모델·구조 변경\n\n### 3-1. 모델 A/B: A.X-4.0-Light 유지 강화 vs Qwen3 4B/8B\n- **무엇**: 국내 3사(LG EXAONE·KT Mi:dm·네이버 SEED) 전수 확인 결과 체급+Ollama+도구 호출 세 조건을 모두 만족하는 후보가 없으므로, 실질 구도는 양자다. `eval_ax7b_shadow.py` 154건으로 qwen3:8b(5.2GB)·qwen3:4b-2507(2.5GB, non-thinking)을 현행 A.X 상징 의도 경로와 비교. 판단 기준은 '상징 의도 JSON 정확도'로 통일, thinking은 꺼서 대화 지연 억제. 병행으로 A.X 모델 카드가 function calling을 정식 지원하므로 Ollama Modelfile 채팅 템플릿의 tools 전달 여부를 확인하고 프롬프트-JSON vs 네이티브 tools를 같은 154건으로 A/B.\n- **근거**: Qwen3 기술보고서 BFCL v3 4B 57.6~65.9 / 8B 60.2~68.1(arxiv 2505.09388), A.X-4.0-Light 모델 카드(KMMLU 64.15, FC 문서화, huggingface.co/skt). Kanana 1.5 8B는 한국어 FC 튜닝의 실존 증거지만 KMMLU 48.28로 교체 순이득 아님.\n- **예상 효과**: 교체는 측정이 이길 때만. 현행이 이기면 \"확인했고 유지한다\"는 기록 자체가 자산(§1).\n- **메모리 정책 명문화**: 8GB 기기 qwen3:4b까지, 16GB 기기 8b 기본, 14b(9.3GB+KV캐시)는 Excel+Tauri 동시 구동 실측 후에만.\n\n### 3-2. 다중 표 시트에 2단 관측(Chain of Spreadsheet)\n- **무엇**: used_range 안에 표가 2개 이상 감지되면 1단계에서 질의 관련 표 영역만 식별(규칙 우선, 애매하면 범용 모델)하고 2단계 프롬프트에는 그 영역 다이제스트만 넣는다. `excel_selection_context.py`(사용자 선택 영역)가 자연스러운 진입점.\n- **근거**: SpreadsheetLLM CoS — 영역 식별→재제시 2단으로 QA +22%p(GPT-4 기준, arxiv 2407.09025). SheetAgent에서 '질의 관련 서브뷰 제공'(Informer) 제거가 -18%p로 최대 하락 요인(arxiv 2403.03636) — **무엇을 보여 주느냐가 모델 교체보다 측정된 효과가 크다.**\n- **예상 효과**: 다중 표·대형 사용범위 파일에서 엉뚱한 열·범위 오지정 감소. 수치는 GPT-4 기준이므로 자체 측정으로 확인.\n\n### 3-3. 다단계 요청용 초소형 DSL (의도 JSON의 시퀀스)\n- **무엇**: 단일 의도 JSON으로 부족한 다단계 요청이 실사용에서 유의미하게 늘면, 자유 Python/VBA 생성이 아니라 **의도 JSON의 배열**(ODSL식 초소형 DSL)을 범용 모델에 생성시키고 기존 `excel_macro_planner` 자리에서 결정적으로 순차 트랜스파일. 구조화 출력 스키마의 배열 확장이므로 1-1의 연장선이다.\n- **근거**: Semantic Interpreter의 DSL+결정적 트랜스파일러(arxiv 2306.03460), SheetCopilot의 액션 API > 자유 코드 실증(arxiv 2305.19308).\n- **예상 효과**: 복잡 요청 커버리지 확장 — 단 2-1 폐루프와 2-4 되묻기 경로가 먼저 있어야 실패가 조용히 쌓이지 않는다. 착수 조건: 다단계 요청 비중을 로그로 실측한 뒤.\n\n---\n\n## 우선순위 요약\n\n| 순서 | 항목 | 막는 한계 | 비용 | 보장 근거 강도 |\n|---|---|---|---|---|\n| 1 | 1-1 구조화 출력 + enum | (a) 발명·붕괴 | 코드 한 곳 | 로컬 실측 완료, 형식은 구조적 보장 |\n| 2 | 2-1 검증 실패 되먹임 폐루프 | (b) 복잡 요청 | 1~2일 | 학계 ablation 최대 단일 요인 |\n| 3 | 2-4 되묻기·거절 경로 + Slot/Relevance 축 | (b)(c) | 1~2일 | 업계 출시 관행 + 한국어 전용 벤치 실존 |\n| 4 | 2-3 프리뷰-승인 UI | 신뢰 | 1~2일 | MS·Google 공통 출시 패턴 |\n| 5 | 2-2 사전 사후조건 | (b) | 1일 | MS 패턴(수치 미공개) |\n| 6 | 1-2, 2-5, 3-2 다이제스트 개선군 | (b) | 소~중 | 소형 모델 이득 실측 있으나 자체 게이트 필수 |\n| 7 | 3-1 모델 A/B | (a)(c) | 평가 15분×수회 | 측정이 이길 때만 교체 |\n| 8 | 3-3 초소형 DSL | (b) | 중 | 착수 조건부(다단계 수요 실측 후) |\n\n공통 규칙: 모든 변경은 `run_command_diagnostics`/`eval_ax7b_shadow` 전후 실측 + 실행 id와 함께 `개발일지.md` 기록. 프롬프트·다이제스트 포맷 변경은 §3.5(SFT 동일 함수 주의)와 TabVerse의 '포맷 승자는 모델마다 다름' 경고에 따라 외부 결론 이식 금지, 자체 측정으로만 채택.",
    "merged": [
      {
        "angle": "products",
        "findings": [
          {
            "claim": "Microsoft의 Semantic Interpreter(Office Copilot 계열 연구)는 LLM에게 자유 코드나 원시 API 호출이 아니라 ODSL(Office Domain Specific Language)이라는 제한된 고수준 DSL 프로그램을 생성시키고, 그 프로그램을 결정적 트랜스파일러가 실제 애플리케이션 API로 변환해 실행한다. 논문은 'LLM은 의도 이해에는 뛰어나지만 애플리케이션별 의도 수행에는 충분치 않다'고 명시하며, Analysis-Retrieval 방식으로 발화별 관련 DSL 문법·예제만 프롬프트에 골라 넣는다.",
            "source": "https://arxiv.org/abs/2306.03460",
            "relevance": "우리의 '상징 의도 JSON + 결정적 바인더' 분리와 정확히 같은 구조를 Microsoft가 제품 연구로 검증했다. LLM 출력은 제한된 중간 표현으로, 좌표·수식 조립은 결정적 코드로 — 우리 방향이 업계 수렴점과 일치한다는 근거."
          },
          {
            "claim": "Google Sheets의 Gemini 편집 기능(2025년 5월 출시분)은 자유 생성이 아니라 5종의 닫힌 액션(드롭다운 생성, 조건부 서식, 피벗 테이블, 필터, 정렬)만 지원하며, 실행 전 '액션 프리뷰 카드'를 띄워 사용자가 Apply를 눌러야 적용되고, 적용 후 undo·설정 조정 버튼을 제공한다.",
            "source": "https://workspaceupdates.googleblog.com/2025/05/use-gemini-google-sheets-advanced-actions.html",
            "relevance": "프론티어 모델을 쓰는 Google조차 상용 출시는 소수의 닫힌 액션 어휘 + 실행 전 확인으로 시작했다. 7B 로컬 모델인 우리가 액션 어휘를 작게 유지하고 어휘 밖 요청은 명확화 질문으로 떨어뜨리는 설계가 업계 표준과 부합한다."
          },
          {
            "claim": "Google은 2026년 3~4월 Gemini in Sheets의 에이전트형 편집을 발표했다. 복잡한 작업은 먼저 '승인용 계획'을 만들어 사용자에게 보여준 뒤 실행하며, 실사용 스프레드시트 벤치마크 SpreadsheetBench에서 70.48% 성공률(인간 전문가 71.33% 직전, 2위 Kingsoft Qingqiu 69.96%)을 기록했다.",
            "source": "https://workspaceupdates.googleblog.com/2026/04/build-and-edit-complex-spreadsheets-with-Gemini-in-Google-Sheets.html",
            "relevance": "개방형(오픈엔디드) 스프레드시트 편집은 프론티어급 모델도 70% 수준이 상한이다. 우리의 96~100%는 과제를 '의도 추출'로 좁혔기에 나온 수치이므로, 7B로 개방형 에이전시를 흉내내는 방향은 피하고 범위를 좁게 유지한 채 액션 어휘를 점진 확장하는 것이 옳다."
          },
          {
            "claim": "Microsoft Agent Mode in Excel(2025년 9월 발표)은 추론·반성(reasoning and reflection) 루프로 워크북과 도구 호출로 상호작용하며, 실행 전에 기대 결과를 정의하는 경량 테스트를 먼저 생성해 각 단계의 '검증 가능한 가드레일'로 삼고, 결과가 의도와 다르면 수정·재시도한다. Excel 함수·객체·전용 도구 호출의 압축 문서를 컨텍스트에 넣는다. SpreadsheetBench 성적은 57.2%(인간 71.3%)로 자체 공표했다.",
            "source": "https://www.microsoft.com/en-us/microsoft-365/blog/2025/09/29/vibe-working-introducing-agent-mode-and-office-agent-in-microsoft-365-copilot/",
            "relevance": "우리 excel_result_verifier(사후조건 검증)·excel_step_repair(재시도 전 보정)와 같은 축이다. Microsoft가 추가로 한 것은 '실행 전에 기대 결과(테스트)를 먼저 정의'하는 순서 — 태스크 유형별 사후조건을 바인더 단계에서 미리 산출해 verifier에 넘기는 개선으로 지금 차용 가능하다."
          },
          {
            "claim": "비(非)에이전트 Excel Copilot 편집은 테이블 형식 데이터에서만 동작하고, 변경 대상 셀을 보라색 하이라이트로 미리 보여준 뒤 사용자가 검토·수락·undo 하는 프리뷰-승인 흐름을 쓴다(정렬·필터·강조·수식 열·차트·피벗 등 한정된 작업군).",
            "source": "https://support.microsoft.com/en-us/office/frequently-asked-questions-about-editing-with-copilot-in-excel-1cfd906d-40b4-46be-8e2d-65b893e28a02",
            "relevance": "'영향 범위를 시각적으로 보여주고 승인받는 것'이 상용 제품의 신뢰 확보 수단이다. 모델 정확도를 더 짜내는 것보다 CONFIRM 등급 작업에 영향 범위 프리뷰 UI를 붙이는 편이 저렴하게 체감 신뢰를 올린다."
          },
          {
            "claim": "SheetCopilot(NeurIPS 2023)은 스프레드시트 기능을 '원자 액션'(가상 API) 집합으로 추상화하고, 상태기계(관측→제안→수정→실행) 루프로 실행하며 소프트웨어 실행 오류를 피드백으로 받아 재계획한다. 221개 과제에서 단일 생성 44.3% 성공으로, VBA 등 자유 코드 생성 베이스라인을 큰 차이로 이겼다.",
            "source": "https://arxiv.org/abs/2305.19308",
            "relevance": "'제한된 액션 어휘가 자유 코드 생성을 이긴다'는 학계의 직접 실증 — 우리 선택(액션 API 방식)의 근거. 또한 복잡 요청(한계 b)에는 한 번에 전체 계획이 아니라 관측→원자 액션 1개 제안→검증→오류 텍스트 되먹임의 폐루프가 검증된 패턴이다."
          },
          {
            "claim": "Ollama는 JSON 스키마로 모델 출력을 제약하는 structured outputs를 공식 지원한다(format 파라미터에 스키마 전달). 스키마를 문법으로 컴파일해 디코딩 시점에 문법을 깨는 토큰의 확률을 0으로 만들므로 형식이 깨진 JSON 자체가 후보에서 배제된다.",
            "source": "https://ollama.com/blog/structured-outputs",
            "relevance": "우리 근본 한계 (a) '없는 액션 발명·형식 붕괴'의 직접 해법. task 필드를 enum으로 선언하면 어휘 밖 액션은 토큰 수준에서 생성 불가능해진다. 프롬프트 지시가 아니라 디코더 제약이므로 7B·한국어 변이에도 형식은 100% 보장된다."
          },
          {
            "claim": "Ollama의 OpenAI 호환 엔드포인트(/v1/chat/completions)에서 response_format json_schema가 무시된다는 이슈(#10001, 2025-03-26)는 메인테이너가 두 방식 모두 정상 동작함을 재현 스크립트로 확인했고, 원인이 Open WebUI 쪽임이 밝혀져 하루 만에 닫혔다(2025-03-27 closed).",
            "source": "https://github.com/ollama/ollama/issues/10001",
            "relevance": "우리 사이드카의 단일 LLM 경로가 OpenAI 호환 API이므로, 경로 변경 없이 기존 /v1/chat/completions 호출에 response_format(json_schema)만 추가하면 구조화 출력 제약을 켤 수 있다는 확인. 네이티브 /api/chat의 format 파라미터는 예비 경로."
          }
        ],
        "actionable": [
          "상징 의도 JSON의 스키마(task를 enum으로, range/열이름/옵션 필드 명시)를 JSON Schema로 작성해, 기존 /v1/chat/completions 호출에 response_format(json_schema)로 전달한다. 디코딩 제약으로 '없는 액션 발명'과 '형식 붕괴'(한계 a)가 토큰 수준에서 차단된다. 적용 전후를 run_command_diagnostics.py --label before/after-structured-output으로 실측한다. 주의: build_planner_prompt는 SFT 생성과 같은 함수이므로 이 변경은 플래너(ax7bplanner)가 아니라 범용 모델(A.X) 경로에만 적용한다.",
          "액션 어휘는 작고 닫힌 집합으로 유지한다(Google도 5종 + 프리뷰로 출시). 스키마 enum 밖 의도가 감지되면 자유 생성으로 확장하지 말고 명확화 질문 또는 '지원 안 함' 응답으로 떨어뜨린다 — tool_registry.py의 권한 체계와 같은 층에서 관리.",
          "CONFIRM 등급 작업에 '영향 범위 프리뷰 → 승인 → 실행' 흐름을 붙인다(Excel Copilot의 보라색 하이라이트, Sheets의 액션 프리뷰 카드 패턴). 바인더가 이미 실제 좌표를 확정하므로 실행 전에 대상 범위·작업 요약을 WorkspacePage에 카드로 보여주는 것은 추가 모델 비용 없이 가능하다.",
          "복잡 요청(한계 b)은 SheetCopilot의 상태기계 패턴으로: 관측(기존 워크북 다이제스트) → 원자 액션 1개 제안 → 검증 → 실패 시 오류 텍스트를 프롬프트에 되먹여 재제안. 현재 excel_step_repair의 단발 보정을 넘어, excel_result_verifier 실패를 재계획 루프의 입력으로 연결한다. Microsoft 패턴대로 태스크 유형별 기대 사후조건을 바인더 단계에서 미리 산출해 verifier에 넘기면 검증이 결정적이 된다.",
          "범위 설정의 기준선: 개방형 스프레드시트 편집은 프론티어 에이전트도 SpreadsheetBench 57~70%(인간 71%)에 머문다. 7B로 개방형 에이전시를 시도하지 말고, 96~100%가 나오는 '의도 추출 + 결정적 조립' 범위를 지키며 액션 enum을 한 개씩 늘릴 때마다 154건 회귀 평가로 게이트한다.",
          "장기 방향: 단일 의도 JSON으로 부족한 다단계 요청이 늘면, 자유 Python/VBA 생성이 아니라 ODSL식 초소형 DSL(의도 JSON의 시퀀스)을 범용 모델에 생성시키고 결정적 트랜스파일(기존 excel_macro_planner 자리)로 확장한다 — 코드 생성 대비 우위는 SheetCopilot이 실증했다."
        ]
      },
      {
        "angle": "papers",
        "findings": [
          {
            "claim": "SheetCopilot(NeurIPS 2023)은 원자 액션 집합 + 상태머신(observe-propose-revise-act) 루프로 GPT-3.5에서 Exec@1 87.3% / Pass@1 44.3%를 달성 — VBA 코드 직접 생성 베이스라인(77.8% / 16.3%) 대비 Pass@1 약 2.7배. Ablation에서 에러 피드백 단독이 Exec@1을 92.8%까지 올린 최대 단일 요인이고, 액션 문서+사용 예시 추가가 Exec@1 +17.2%p, Pass@1 +10%p.",
            "source": "https://arxiv.org/abs/2305.19308",
            "relevance": "우리 구조(고정 액션 어휘의 상징 의도 JSON + excel_step_repair/excel_result_verifier 피드백)가 학계 실측과 일치함을 확인. 특히 '실패 시 오류 메시지를 그대로 모델에 되돌리는 루프'가 가장 큰 단일 기여 요인이므로, 검증 실패 결과가 재계획 프롬프트에 실제로 들어가는지가 투자 우선순위."
          },
          {
            "claim": "SheetAgent(2024)는 Planner(코드) + Informer(시트의 태스크 관련 서브뷰만 SQL로 추출) + Retriever(오류 시 스니펫 검색)로 SheetCopilot 벤치마크에서 Pass@1 61.1% vs 44.3%(+16.8%p). SheetRM ablation에서 Informer 제거 시 Pass@1 31.2%→13.2%(-18%p)로 세 컴포넌트 중 최대 하락 — '전체 시트가 아니라 질의 관련 서브뷰를 보여 주는 것'이 가장 큰 정확도 지렛대였다.",
            "source": "https://arxiv.org/abs/2403.03636",
            "relevance": "우리 excel_selection_context / 워크북 다이제스트가 Informer의 역할. 7B가 복잡 요청을 이해 못 하는 문제(한계 b)의 상당 부분은 모델이 아니라 '무엇을 보여 주느냐'로 풀린다는 실측 — 관측 서브뷰 추출 품질에 투자하는 것이 모델 교체보다 측정된 효과가 크다."
          },
          {
            "claim": "SpreadsheetLLM/SheetCompressor(Microsoft)는 구조 앵커 추출 + 역색인 변환 + 동일 데이터형식 범위 집계로 평균 24.79배 압축(토큰 96% 절감)하면서 표 탐지 F1을 올렸고, 이득이 소형 모델에서 가장 컸다: Llama2 +38%, Phi3 +36%, Llama3 +25%, Mistral-v2 +18% (GPT-4는 약 +26%).",
            "source": "https://arxiv.org/abs/2407.09025",
            "relevance": "시트 인코딩 압축은 7B급에서 이득이 가장 크다는 직접 증거. 워크북 다이제스트를 셀 단위 나열이 아니라 '머리글·경계 앵커 + 동일 형식 범위 집계' 방식으로 쓰면 컨텍스트가 줄면서 정확도가 오른다 — 우리 excel_selection_context 다이제스트 포맷 설계에 바로 적용 가능."
          },
          {
            "claim": "TableGPT2는 7B 모델을 59.4만 테이블 / 236만 질의-테이블-출력 튜플로 학습해 23개 표 벤치마크 평균 +35.2%를 얻었다 — 표 특화 7B가 성립하려면 우리 SFT(1000건)의 세 자릿수 배 규모 데이터가 필요했다.",
            "source": "https://arxiv.org/abs/2411.02059",
            "relevance": "1000건 SFT 플래너가 파라미터를 암기(=SUM(E:E) 등)하고 58~67%에 머문 우리 실측과 정합. 학계도 소규모 SFT로 특화 플래너를 만들지 않는다 — 범용 인스트럭트 모델 + 상징 의도 JSON + 결정적 바인더 노선이 옳다는 외부 근거이며, 플래너 SFT 추가 투자를 중단할 근거."
          },
          {
            "claim": "SpreadsheetBench(NeurIPS 2024)는 실제 엑셀 포럼 질의 912건 벤치마크에서 최고 모델(GPT-4o, Excel Copilot)도 약 20% vs 인간 71%였고, 오픈소스 모델은 한 자릿수 %에 그쳤다.",
            "source": "https://arxiv.org/abs/2406.14991",
            "relevance": "복잡 실사용 요청은 프런티어 모델도 못 푸는 미해결 문제 — 7B로 이를 프롬프트 개선으로 뚫으려는 시도는 헛수고라는 보정치. 제품 범위를 검증 가능한 명령 분포로 좁히고, 못 다루는 요청은 excel_macro_planner 분해 또는 되묻기로 라우팅하는 것이 학계 실측이 가리키는 방향."
          },
          {
            "claim": "XGrammar-2는 문법 제약 디코딩(JSON 스키마 강제)으로 BFCL 함수 호출에서 Llama-3.2-3B를 33.12%→77.75%(스키마 유효율 40.7%→100%)로 올렸고, 제약된 3B가 무제약 70B(45.6%)를 이겼다 — 형식 붕괴·잘못된 호출 제거가 주 메커니즘.",
            "source": "https://arxiv.org/pdf/2601.04426",
            "relevance": "우리 한계 (a) '없는 액션 발명·JSON 형식 붕괴'의 직접 해법. Ollama는 format/response_format에 JSON 스키마를 받으므로(llama.cpp 문법 샘플링), task 필드를 실제 액션 enum으로 제약하면 없는 액션 발명이 디코딩 단계에서 원천 차단된다."
          },
          {
            "claim": "The Constraint Tax는 반대 증거: 0.5~1.7B 모델에서 하드 제약이 스키마 유효율을 61.5%→100%로 올리는 동시에 답 정확도를 19.7%→11.0%로 떨어뜨렸고, '스키마는 유효하나 내용이 틀린' 출력이 49.5%→88.9%로 급증했으며 한 툴콜 태스크는 91.5%→48.0%로 추락했다.",
            "source": "https://arxiv.org/pdf/2605.26128",
            "relevance": "제약 디코딩 도입 시 §2 프로토콜대로 before/after를 재야 하는 이유. 특히 '유효-but-오답' 비율을 별도 지표로 추적해야 한다 — 제약은 눈에 보이는 형식 실패를 조용한 오답으로 바꿀 수 있다. (우리는 7B이고 이미 96~100%라 XGrammar-2의 3B 이득 영역에 가깝지만, 측정 없이 단정하면 안 된다.)"
          }
        ],
        "actionable": [
          "Ollama 구조화 출력 적용: 범용 모델의 상징 의도 JSON 호출에 response_format(JSON 스키마)을 넘기고 task 필드를 실제 액션 enum으로 제약 — 없는 액션 발명과 형식 붕괴를 디코딩 단계에서 차단 (XGrammar-2: 3B에서 33→78%). 단, run_command_diagnostics before/after 측정 필수이며 '스키마 유효하지만 내용 오답' 비율을 새 지표로 추가 (Constraint Tax 경고).",
          "excel_selection_context의 태스크 관련 서브뷰 추출을 최우선 투자처로: SheetAgent에서 Informer 제거가 -18%p로 최대 하락 요인. 전체 시트 덤프 대신 질의에 언급된 열·범위 중심 서브뷰만 프롬프트에 넣는 방향으로 다이제스트를 다듬는다.",
          "워크북 다이제스트에 SheetCompressor 3기법 적용: 머리글·표 경계 구조 앵커, 동일 형식 연속 범위의 집계 표기(셀 나열 금지), 반복 값 역색인 — 소형 모델일수록 이득이 크다(+18~38%)는 실측이 있으므로 7B에 정확히 해당.",
          "excel_result_verifier의 실패 내용(오류 메시지·사후조건 불일치)이 재계획 프롬프트에 원문 그대로 들어가는지 확인하고 없으면 연결: SheetCopilot ablation에서 에러 피드백이 Exec@1 최대 단일 기여 요인(92.8%).",
          "플래너 SFT 추가 투자 중단 근거 확보: 표 특화 7B가 성립한 사례(TableGPT2)는 236만 튜플 규모 — 1000건 SFT의 암기 현상은 우리만의 실패가 아니라 규모의 문제. 범용 모델 + 상징 의도 + 바인더 노선을 유지하고, 남는 리소스를 위 1~4번에 돌린다.",
          "복잡 요청 기대치 보정: SpreadsheetBench에서 프런티어 모델도 ~20%인 영역은 7B로 정면 돌파하지 않는다. excel_macro_planner 분해가 커버 못 하는 요청은 되묻기(확인 질문)로 라우팅하는 명시적 경로를 만든다."
        ]
      },
      {
        "angle": "constrained",
        "findings": [
          {
            "claim": "Ollama 0.5.0(2024-12-06)부터 format 필드에 JSON 스키마를 받아 출력을 문법 수준으로 제약하는 구조화 출력을 지원하며, 공식 블로그가 OpenAI 호환 엔드포인트(client.parse + response_format) 사용 예시를 직접 보여준다.",
            "source": "https://ollama.com/blog/structured-outputs",
            "relevance": "우리 LLM 경로는 Ollama OpenAI 호환 API(/v1/chat/completions) 단일 경로다. 별도 라이브러리(outlines 등) 도입 없이 이미 쓰는 API의 필드 하나로 제약 디코딩을 켤 수 있다."
          },
          {
            "claim": "로컬 실측(이 머신, Ollama 0.32.14 + ax4-light): /v1/chat/completions에 response_format {\"type\":\"json_schema\"}를 보내면 실제로 문법이 강제된다 — 인위적 enum 토큰 \"ZQ_SORT_ROWS\"와 임의 필드명 task_zx/target_col_zx를 바이트 그대로 출력했다(7B 모델이 자발적으로 낼 수 없는 문자열).",
            "source": "https://docs.ollama.com/openai",
            "relevance": "공식 문서는 response_format 지원을 체크박스로만 표기해 json_schema 지원이 불명확했는데, 우리 버전·우리 모델·우리 엔드포인트에서 동작을 직접 확인했다. 한계 (a) '없는 액션 발명·형식 붕괴'를 디코더 수준에서 차단할 수 있다."
          },
          {
            "claim": "구현 원리: Ollama가 JSON 스키마를 GBNF 문법으로 변환해 llama.cpp에 넘기고, 샘플링 때 문법상 무효한 토큰의 로짓을 -INFINITY로 마스킹한다. 모델 자신은 문법의 존재를 모른다.",
            "source": "https://blog.danielclayton.co.uk/posts/ollama-structured-outputs/",
            "relevance": "모델이 스키마를 '못 보기' 때문에 프롬프트에서 스키마를 설명하는 일은 여전히 필수다. 실측에서도 의미 없는 enum을 주자 sum 요청에 ZQ_SORT_ROWS(정렬)를 골랐다 — 제약은 형식만 보장하고 의미 선택은 프롬프트 몫이다."
          },
          {
            "claim": "dottxt의 'Say What You Mean'(\"Let Me Speak Freely\" 반박): 원논문은 프롬프트 불일치·스키마 없는 JSON 모드·LLM 파서 등 방법론 결함이 있었고, Llama-3-8B-Instruct로 조건을 맞춰 재실험하면 구조화 생성이 전 과제에서 같거나 더 높다(GSM8K 0.77→0.78, Last Letter 0.73→0.77, Shuffled Objects 0.41→0.44).",
            "source": "https://blog.dottxt.ai/say-what-you-mean.html",
            "relevance": "8B급 재실험이라 우리 7B급에 직접 이식 가능한 증거다. '제약이 모델 성능을 떨어뜨린다'는 우려 없이 도입해도 된다 — 단, 프롬프트와 스키마가 정합해야 한다는 조건이 붙는다."
          },
          {
            "claim": "원논문 'Let Me Speak Freely?'(arXiv 2408.02442)는 형식 제약이 추론 성능을 떨어뜨린다고 주장했으나, 그 '구조화' 조건은 실제 제약 엔진 없이 프롬프트로만 JSON을 시킨 것이었다.",
            "source": "https://arxiv.org/html/2408.02442v1",
            "relevance": "우리가 지금까지 쓴 방식(프롬프트로 JSON 요청 + json_object 모드)이 정확히 이 '나쁜 조건'이다. 이 논문이 측정한 성능 저하는 제약 디코딩의 문제가 아니라 우리가 겪는 형식 붕괴 문제 그 자체다."
          },
          {
            "claim": "JSONSchemaBench(arXiv 2501.10868, 실제 스키마 1만 건): 6개 엔진(Guidance/Outlines/llama.cpp GBNF/XGrammar/OpenAI/Gemini) 비교에서 제약 디코딩이 다운스트림 과제 성능을 최대 4% 일관되게 개선하고 생성을 최대 50% 가속했다. 단 엔진 간 스키마 커버리지가 최대 2배 차이 난다.",
            "source": "https://arxiv.org/html/2501.10868v1",
            "relevance": "업계 전반(OpenAI structured outputs 포함)이 같은 방식으로 이 문제를 풀었다는 확인. 커버리지 편차 교훈: 스키마를 얕게(object+string+enum+array) 유지해야 한다 — 우리 상징 의도 JSON이 정확히 그 모양이다."
          },
          {
            "claim": "로컬 실측: regex pattern 키워드도 강제된다 — 프롬프트로 'D:D 형식으로 답해'라고 지시했는데 pattern이 숫자를 요구하자 문법이 지시를 눌러 D2:D100을 출력했다.",
            "source": "https://blog.danielclayton.co.uk/posts/ollama-structured-outputs/",
            "relevance": "강제가 사용자 의도를 조용히 덮어쓸 수 있다는 뜻. pattern은 '어떤 적합값이든 수용 가능한 자리'에만 쓰고, 좌표·수식 조립은 계속 결정적 바인더가 소유해야 한다."
          },
          {
            "claim": "로컬 실측: 제약 출력 안에서 한국어 문자열(\"매출\", \"비용\", \"E열\")이 정상 생성되고, 속성 출력 순서는 스키마 순서와 다르게 나온다(순서 미강제). 필수 아닌 필드는 생략될 수 있다.",
            "source": "https://ollama.com/blog/structured-outputs",
            "relevance": "한계 (c) 한국어: 열 이름을 한국어 그대로 담는 상징 의도에 문제없다. 단 dottxt 권고인 '추론 필드를 답 앞에' 배치는 문법이 보장하지 못하므로 few-shot 예시로 유도해야 한다."
          },
          {
            "claim": "알려진 실패 모드: max_tokens로 생성이 중간에 끊기면 문법에도 불구하고 미완성 JSON이 나올 수 있고, 문법 토큰 마스킹은 병렬화가 안 돼 추론이 느려질 수 있다. Ollama 공식 권고는 temperature 0 + 프롬프트에 'JSON으로 답하라' 명시.",
            "source": "https://blog.danielclayton.co.uk/posts/ollama-structured-outputs/",
            "relevance": "finish_reason != \"stop\"을 실패로 처리하는 가드가 필요하고, 제약 도입 후에도 사후 파싱 검증(현 validator)을 제거하면 안 되는 이유다."
          }
        ],
        "actionable": [
          "ollama_service.py:53의 chat()에 `json_schema: dict | None = None` 파라미터 추가: 주어지면 94행의 json_object 대신 payload[\"response_format\"] = {\"type\":\"json_schema\",\"json_schema\":{\"name\":\"excel_intent\",\"schema\":json_schema}} 전송. 이 머신의 Ollama 0.32.14에서 동작 실측 완료(코드 변경은 이 한 곳). json_only는 폴백으로 유지.",
          "첫 적용 지점은 excel_intent_normalizer.py:116(96~100% 상징 의도 경로): task 필드를 '구현된 액션만 담은 enum'으로 강제하면 없는 액션 발명이 디코더 수준에서 불가능해진다. 이후 excel_live_agent.py:1089, excel_macro_planner.py:457로 확대.",
          "스키마 원천은 excel_tool_schemas.py의 _clean_schema(288행) 산출물을 재사용 — 도구 스키마와 제약 스키마가 한 소스에서 나오게 유지(§4 원칙).",
          "제약을 켜도 프롬프트를 줄이지 말 것: 실측에서 불투명한 enum을 주자 유효하지만 틀린 값을 골랐다. enum 값은 자기설명적 이름 + 각 액션의 한국어 한 줄 설명을 프롬프트에 계속 포함한다.",
          "복잡 요청 이해(한계 b) 보완: dottxt 권고대로 '왜 이 task인지' 추론 문자열 필드를 스키마 첫 속성으로 넣고 few-shot 예시로 순서를 유도한다(Ollama 문법은 속성 순서를 강제하지 않음을 실측).",
          "가드 추가: finish_reason != \"stop\"이면 절단 JSON으로 간주해 실패 처리하고 max_tokens를 여유 있게. temperature 0 유지. 기존 사후 파싱·검증(validator/binder)은 제거하지 않는다 — 스키마는 형식만 보장하고 시트 정합성(열 존재 등)은 못 본다.",
          "pattern·oneOf 같은 복잡 키워드는 쓰지 않는다: 엔진 간 커버리지 편차가 크고(최대 2배), 실측에서 pattern이 사용자 지시를 조용히 덮어썼다. 좌표·수식은 계속 바인더가 조립.",
          "도입 전후를 §2 절차로 측정: run_command_diagnostics before/after + eval_ax7b_shadow에 제약 on/off 팔 추가. 특히 '형식 붕괴로 인한 재시도 횟수'와 '발명 액션 발생률'의 전후 변화를 실행 id와 함께 개발일지에 남긴다."
        ]
      },
      {
        "angle": "models",
        "findings": [
          {
            "claim": "Qwen3 공식 기술보고서 기준 BFCL v3 점수는 4B 65.9/57.6, 8B 68.1/60.2, 14B 70.4/61.5 (thinking/non-thinking) — 7B급 오픈 모델 중 도구 호출 최상위권이다.",
            "source": "https://arxiv.org/pdf/2505.09388",
            "relevance": "우리 '상징 의도 JSON' 역할을 맡길 대체·비교 후보. 현재 A.X-4.0-Light와 같은 체급(4B/8B)에서 도구 호출이 사전학습부터 네이티브라, 프롬프트로 JSON을 강제할 때의 액션 발명 위험(한계 a)이 구조적으로 낮다. 단 thinking 모드 점수가 8점가량 높으므로 대화형 엑셀 편집의 지연과 트레이드오프."
          },
          {
            "claim": "Ollama 공식 qwen3 라이브러리는 tools 캐파블 태그이며 Q4 다운로드 크기가 4b 2.5GB, 8b 5.2GB, 14b 9.3GB다. 4b 태그는 256K 컨텍스트로, 도구 사용이 개선된 2507 리프레시(BFCL-v3 61.9, non-thinking)가 반영돼 있다.",
            "source": "https://ollama.com/library/qwen3 , https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507/discussions/18",
            "relevance": "8~16GB RAM 전제에서 실제로 올릴 수 있는 것: 4b는 8GB 기기에서도 동작, 8b는 16GB에서 여유, 14b(9.3GB+KV캐시)는 Excel+Tauri가 같이 떠 있는 16GB 기기에서 한계선. 4B-2507은 non-thinking인데도 구형 4B thinking에 근접한 도구 호출 점수라 GB당 정확도가 가장 좋다."
          },
          {
            "claim": "카카오 Kanana 1.5 8B(Apache 2.0, 32K ctx, GGUF 커뮤니티 양자화로 Ollama 구동 가능)는 한국어 도구 호출 벤치마크 FunctionChat-Bench에서 58.00을 기록(전 버전 17.37에서 대폭 개선)했으나 KMMLU는 48.28이다.",
            "source": "https://huggingface.co/kakaocorp/kanana-1.5-8b-instruct-2505",
            "relevance": "'한국어 함수 호출 튜닝이 된 8B'가 실존한다는 증거. 다만 한국어 지식(KMMLU 48.28)이 현재 쓰는 A.X-4.0-Light(64.15)보다 낮아 교체가 순이득이 아니다 — 우리에게 가치 있는 건 모델보다 이 벤치마크 쪽이다."
          },
          {
            "claim": "FunctionChat-Bench(카카오)는 한국어 도구 사용 대화 전용 벤치마크로, SingleCall 500건 + 멀티턴 45개 시나리오를 Call/Completion/Slot(누락 파라미터 되묻기)/Relevance(도구로 못 푸는 요청 거절) 4축으로 채점하며, OpenAI 호환 엔드포인트(로컬 vLLM/자체 API 포함)에 바로 돌릴 수 있다.",
            "source": "https://github.com/kakao/FunctionChat-Bench , https://arxiv.org/html/2411.14054v1",
            "relevance": "우리 Ollama OpenAI 호환 경로에 그대로 꽂아 후보 모델의 '한국어 도구 호출' 성능을 자체 154건 평가와 별도 축으로 잴 수 있다. 특히 Slot/Relevance 축은 우리 excel_param_binder의 파라미터 확정 실패·거절 처리와 정확히 같은 문제를 측정한다."
          },
          {
            "claim": "현재 사용 중인 A.X-4.0-Light(7B, Apache 2.0)의 공식 모델 카드가 function calling을 정식 지원 기능으로 문서화하고 있으며(슬롯 필링 포함 예제 수록), KMMLU 64.15로 같은 체급 대비 한국어가 가장 강하고 한국어 토큰 효율이 GPT-4o 대비 약 33% 좋다.",
            "source": "https://huggingface.co/skt/A.X-4.0-Light",
            "relevance": "모델을 바꾸지 않아도 이미 도구 호출 학습이 된 모델을 쓰고 있다는 뜻이다. 지금은 프롬프트로 JSON을 시키고 있는데, Ollama Modelfile의 채팅 템플릿이 tools 파라미터를 제대로 전달하는지 확인하면 네이티브 함수 호출 경로로 전환할 여지가 있다."
          },
          {
            "claim": "Ollama 구조화 출력(structured outputs)은 format 파라미터에 JSON 스키마를 넣으면 '모델 출력을 스키마가 정의한 형식으로 제약(constrain)'하며, 모델 불문 동작하고 OpenAI 호환 API의 response_format으로도 쓸 수 있다. 공식 문서는 스키마를 프롬프트에도 함께 넣고 temperature 0을 권장한다.",
            "source": "https://ollama.com/blog/structured-outputs , https://docs.ollama.com/capabilities/structured-outputs",
            "relevance": "남은 근본 한계 (a) '없는 액션 발명·형식 붕괴'의 직접 해법이다. task 필드를 enum으로 제약한 스키마를 걸면 존재하지 않는 액션은 디코딩 단계에서 아예 생성이 불가능해진다 — 모델 교체 없이 지금 경로에 바로 적용 가능."
          },
          {
            "claim": "EXAONE 4.0은 에이전틱 도구 사용을 내장했지만 크기가 32B와 1.2B뿐이고(중간 체급 없음) Ollama 공식 지원이 없어 커뮤니티 GGUF로만 구동된다. 구세대 중간 체급인 EXAONE Deep 7.8B는 KMMLU-Pro 40.82(한국어)로 Qwen3-14B(65.40)에 크게 뒤진다.",
            "source": "https://huggingface.co/LGAI-EXAONE/EXAONE-4.0-32B , https://github.com/ollama/ollama/issues/11433 , https://benchlm.ai/leaderboards/korean-benchmarks",
            "relevance": "'한국어 강자'라는 인상과 달리 EXAONE 라인업에는 우리 8~16GB 슬롯에 맞는 도구 호출 가능 체급이 없다. 32B는 Q4로도 약 19GB라 탑재 불가, 1.2B는 플래너 역할에 부족 — 후보에서 제외해도 된다는 실측 근거."
          },
          {
            "claim": "KT Mi:dm 2.0(Base 11.5B + Mini 2.3B, 한국 특화)은 function calling을 지원하지만 공식 가이드가 vLLM + 커스텀 tool-parser 플러그인 경로를 권장하며, Ollama에서는 채팅 구동 가이드만 있고 네이티브 도구 호출 파싱은 검증돼 있지 않다.",
            "source": "https://github.com/K-intelligence-Midm/Midm-2.0 , https://huggingface.co/K-intelligence/Midm-2.0-Base-Instruct",
            "relevance": "한국어 관점에선 매력적인 11.5B(Q4 약 7GB, 16GB 기기 탑재 가능) 후보지만, 우리 단일 경로(Ollama OpenAI 호환)에서 도구 호출 배관을 직접 만들어야 한다. 시험한다면 네이티브 tools가 아니라 구조화 출력(JSON 스키마) 경로로 평가하는 편이 공정하다."
          },
          {
            "claim": "함수 호출 특화 SFT 소형 모델들(ToolACE-8B는 BFCL에서 GPT-4급, xLAM-2-3b-fc-r 65.74%, Hammer-2.1-7B는 GPT-4o-mini 상회)이 BFCL 상위권을 차지하지만, 전부 Llama/Qwen 기반 영어 중심 특화 파인튜닝이다.",
            "source": "https://huggingface.co/Team-ACE/ToolACE-8B , https://arxiv.org/html/2410.04587v2 , https://arxiv.org/pdf/2511.22138",
            "relevance": "업계도 '소형 모델 + 도구 호출 특화 학습'으로 대형 모델을 이기는 패턴을 검증했다 — 단, 이는 우리가 ax7bplanner에서 이미 반증한 '좁은 SFT의 파라미터 암기' 계열과 같은 접근이고 한국어 성능이 미검증이다. 우리 구조(범용 모델 96~100% + 결정적 바인더)가 학계 흐름과 부합하며, 이 특화 모델들을 채택할 이유는 없다는 방증."
          },
          {
            "claim": "네이버 HyperCLOVA X SEED 오픈소스 라인업은 0.5B/1.5B/3B로 도구 선택 능력을 갖췄다고 홍보되지만, 8~16GB 슬롯에서 플래너를 맡길 중간 체급(7~14B)이 없고 Ollama 공식 등록도 없다.",
            "source": "https://huggingface.co/collections/naver-hyperclovax/hyperclova-x-seed",
            "relevance": "한국어 오픈 모델 후보군을 전수 확인한 결과의 일부: 국내 3사(LG/KT/네이버) 중 우리 체급+Ollama+도구 호출 세 조건을 모두 만족하는 것은 없다. 결국 실질 후보는 현행 A.X-4.0-Light 유지 강화 vs Qwen3 4B/8B 교체의 양자 구도다."
          }
        ],
        "actionable": [
          "즉시(모델 교체 없음): 플래너 호출(Ollama OpenAI 호환 /v1/chat/completions)에 response_format으로 상징 의도 JSON 스키마를 걸고 task 필드를 액션 화이트리스트 enum으로 제약한다. '없는 액션 발명·형식 붕괴'(한계 a)가 디코딩 단계에서 차단된다. 적용 전후를 run_command_diagnostics.py -n 3 --label before/after-structured-output으로 잰다. 공식 권장대로 스키마를 프롬프트에도 병기하고 temperature 0을 쓴다.",
          "A.X-4.0-Light의 네이티브 function calling 경로 확인: 모델 카드가 FC를 정식 지원하므로, 현재 Ollama Modelfile의 채팅 템플릿이 tools를 전달·파싱하는지 확인하고, 프롬프트-JSON 방식과 네이티브 tools 방식을 같은 154건으로 A/B 한다.",
          "모델 후보 A/B: qwen3:8b(5.2GB)와 qwen3:4b(2507, 2.5GB, non-thinking)를 eval_ax7b_shadow.py 154건으로 현행 A.X-4.0-Light 상징 의도 경로와 비교한다. thinking은 꺼서(non-thinking) 대화형 편집 지연을 억제하고, 판단 기준은 '상징 의도 JSON 정확도'(우리 바인더 전제)로 통일한다.",
          "한국어 도구 호출 전용 평가축 도입: kakao/FunctionChat-Bench를 우리 Ollama 엔드포인트에 그대로 돌리거나(OpenAI 호환 지원), 최소한 Slot(누락 파라미터 되묻기)/Relevance(도구로 못 푸는 요청 거절) 두 축을 자체 평가셋에 추가한다 — excel_param_binder 실패 유형과 정확히 대응된다.",
          "메모리 정책 명문화: 8GB 기기는 qwen3:4b(2507)까지, 16GB 기기는 8b 기본·14b(9.3GB+KV캐시)는 Excel+Tauri 동시 구동을 실측한 뒤에만 허용. EXAONE 4.0(32B/1.2B만 존재)·HyperCLOVA X SEED(3B 이하)는 체급 불일치로 후보 제외.",
          "채택 보류: ToolACE/xLAM/Hammer류 FC 특화 SFT 모델은 ax7bplanner에서 이미 반증한 '좁은 SFT 암기' 계열이고 한국어 미검증 — 도입하지 않는다. Mi:dm 2.0(11.5B)은 한국어 매력은 있으나 Ollama 도구 호출 배관이 DIY라, 시험하더라도 구조화 출력 경로로만 평가한다.",
          "이 조사와 이후 A/B 결과는 개발일지.md에 실행 id와 함께 기록한다(§1 규칙)."
        ]
      },
      {
        "angle": "encoding",
        "findings": [
          {
            "claim": "SpreadsheetLLM(SheetCompressor)은 셀좌표+값 나열(바닐라 인코딩)이 토큰 폭발로 실패함을 보이고, 구조 앵커 추출(레이아웃 경계 ±4행/열만 유지, 내용 75% 제거·경계 97% 보존) + 역색인(값→주소범위 사전, 빈 셀 제외, 동일값 병합, 4.4×→14.9× 압축)으로 평균 25× 압축하면서 표 감지 F1을 GPT-4 인컨텍스트에서 25.6%p 올렸다.",
            "source": "https://arxiv.org/abs/2407.09025",
            "relevance": "우리 다이제스트(시트·사용범위·열=머리글·예시행 1개·저카디널리티 값 후보)가 문헌이 수렴한 '압축 요약' 방향과 일치함을 확인. 특히 '값→범위 역색인'은 우리 값 후보(categories)의 상위 호환 — 반복값이 많은 실무 시트에서 필터 파라미터 확정 정확도를 올릴 여지가 있다."
          },
          {
            "claim": "SheetCompressor에서 서식 정보(Number Format String 집계)를 넣으면 압축은 14.9×→24.8×로 좋아지지만 F1은 오히려 떨어졌다(제거 시 0.789 vs 포함 시 0.759) — 셀 서식 토큰은 작은 이득 대비 모델을 혼란시킨다.",
            "source": "https://arxiv.org/html/2407.09025v1",
            "relevance": "우리가 '활성 시트만 서식(layout) 표시'로 제한한 결정이 실측으로 뒷받침됨. 다이제스트에 전 시트 서식을 추가하자는 유혹을 근거 있게 거절할 수 있고, 지금 넣는 layout도 전/후 측정 대상이다."
          },
          {
            "claim": "SheetCompressor로 파인튜닝한 7~8B급 오픈 모델(Llama3-8B F1 0.719, Mistral-v2 0.726, Phi3 0.689)이 압축 덕에 25~38% 이득을 얻어 GPT-4 압축판(0.759)에 근접했다 — 인코딩 개선의 수혜는 작은 모델일수록 크다.",
            "source": "https://arxiv.org/abs/2407.09025",
            "relevance": "7B 로컬 모델 전제인 우리 제품에서 '모델을 키우는 것'보다 '보여주는 방식을 고치는 것'이 남은 정확도를 회수하는 지렛대라는 직접 증거."
          },
          {
            "claim": "SUC 벤치마크(Table Meets LLM): GPT-3.5조차 셀 주소 조회 정확도가 39.7~44%, 행/열 개수 세기가 42~67%에 불과 — LLM은 좌표 기반 구조 조작에 근본적으로 약하다. 포맷 중에서는 HTML이 GPT 계열에 최고(파티션 인식 96.7% vs NL+구분자 93%)였다.",
            "source": "https://arxiv.org/html/2305.13062v4",
            "relevance": "'좌표·수식은 결정적 바인더가 조립하고 모델은 머리글 이름만 다룬다'는 우리 아키텍처가 문헌으로 정당화됨. 모델에게 범위 좌표를 계산·선택시키는 어떤 회귀도 막아야 할 이유."
          },
          {
            "claim": "소형 모델(GPT-4.1-nano) 11개 포맷 비교에서 Markdown-KV(레코드마다 '필드명: 값' 나열)가 60.7%로 1위, 마크다운 표 51.9%, CSV 44.3%, 파이프 구분 41.1% — 작은 모델은 위치(열 순서)로 의미를 추론하는 포맷에 특히 약하고, 셀마다 이름표를 붙여주면 16%p 좋아진다(대신 토큰 2.7×).",
            "source": "https://blog.iptek.web.id/posts/2025-10-05-llm-table-format/",
            "relevance": "우리 다이제스트의 '열: C=매출' 표기는 이미 KV형이라 올바른데, '예시행'은 값만 위치 순서로 나열해 소형 모델이 열-값 대응을 놓치기 쉬운 형태다 — 구체적 개선 지점."
          },
          {
            "claim": "TabVerse(2026)와 RealHiTBench(2025)는 포맷 승자가 모델 계열·태스크마다 달라 보편 승자가 없음을 보인다(TabVerse: HTML이 텍스트 파이프라인에서 '대체로 가장 강건', RealHiTBench: GPT-4는 LaTeX 최고) — 포맷 선택은 대상 모델로 직접 재야 한다.",
            "source": "https://arxiv.org/html/2606.09578",
            "relevance": "GPT 기준 결론(HTML 최고 등)을 A.X-4.0-Light에 그대로 이식하면 안 된다는 경고. 우리는 자체 평가 하네스(run_command_diagnostics, planner_eval)가 있으므로 다이제스트 포맷 변경은 반드시 자체 전/후 측정으로 채택한다."
          },
          {
            "claim": "SheetCopilot(NeurIPS 2023)의 절제 실험: 시트 상태 피드백만 넣어도 Exec@1 56.6→75.6%, 오류 피드백만으로 Exec@1 92.8%, 둘 다 결합 시 Pass@1 18.6→44.3% — 매 스텝 후 재관측+오류 텍스트 회신이 프롬프트 문구 개선보다 큰 이득을 준다.",
            "source": "https://arxiv.org/abs/2305.19308",
            "relevance": "관측 루프 설계의 정량 근거. 우리 excel_result_verifier·excel_step_repair가 이 패턴인데, '변이 스텝마다 다이제스트 무효화→다음 호출에서 새 상태 재관측'이 빠짐없이 일어나는지가 정확도를 좌우한다는 뜻."
          },
          {
            "claim": "SheetAgent(WWW 2025)의 Planner는 전체 데이터가 아니라 경량 상태(행/열 수, 머리글, 열별 dtype)만 보고, 근거 데이터가 필요하면 Informer가 SQL로 필요한 부분만 추출해 준다; 복잡 벤치마크(SheetRM)에서 오픈 7~14B 모델은 프레임워크 없이는 Pass@1 0%였다.",
            "source": "https://arxiv.org/pdf/2403.03636",
            "relevance": "(b) 7B의 복잡 요청 한계는 업계도 동일 — 해법은 모델이 아니라 구조(경량 관측 + 필요 시 조준 추출 + 분해)다. 우리 macro_planner(분해)+digest(경량 관측) 구성이 맞고, '질의 관련 행만 뽑아 보여주는 조준 관측'이 아직 없는 조각이다."
          },
          {
            "claim": "SpreadsheetLLM의 Chain of Spreadsheet: 1단계에서 압축 시트 전체로 '질문과 관련된 표 영역'만 식별하고, 2단계에서 그 영역만 다시 넣어 답하게 하면 QA 정확도가 GPT-4 기준 +22%p(0.466→0.743, FT 포함).",
            "source": "https://arxiv.org/html/2407.09025v1",
            "relevance": "한 시트에 표가 여러 개이거나 사용범위가 큰 실무 파일에서, 다이제스트를 '전체 요약 한 방'이 아니라 '영역 식별→그 영역만 재제시' 2단으로 나누면 작은 모델의 오지정(엉뚱한 열·범위)을 줄일 수 있다는 근거."
          }
        ],
        "actionable": [
          "예시행을 KV형으로: c:\\Users\\asdjj\\OneDrive\\바탕 화면\\동화책 프로젝트\\officeclaw\\python-sidecar\\office_claw_sidecar\\services\\excel_workbook_digest.py:254 의 예시행이 값만 위치 순서로 나열된다('예시행: 서울 | 100 | …'). 소형모델 실측(Markdown-KV +16%p)에 따라 '예시행: 지역=서울 | 매출=100'처럼 머리글=값 쌍으로 바꾸고, run_command_diagnostics before/after로 채택 여부를 정한다(§3.5: 프롬프트 형식 변경이므로 측정 필수).",
          "변이 스텝마다 재관측 보장: SheetCopilot 절제(시트 상태 피드백만으로 Exec@1 +19%p) 근거로, excel_live_agent의 다중 스텝 경로에서 쓰기 액션 성공 직후 invalidate_digest_cache(workbook_id)가 항상 호출되고 다음 모델 호출이 갱신된 다이제스트를 받는지 감사한다. 실패 시에는 오류 텍스트 원문 + 갱신 다이제스트를 함께 회신하는지도 확인(오류 피드백 단독이 Exec@1 92.8%로 최대 기여).",
          "값→범위 역색인 실험: 다이제스트의 값 후보(categories)를 SpreadsheetLLM 역색인 방식으로 확장 — 저카디널리티 열에서 '완료=B2:B14,B20' 식으로 값이 어디 있는지까지 붙이는 변형을 만들어, 필터/조건부 명령('완료된 것만') 케이스만 골라 전/후 측정한다. 이득이 없으면 버린다(서식 집계처럼 압축이 정확도를 깎은 전례 있음).",
          "서식 정보 확장 금지 원칙 명문화: SheetCompressor 실측(서식 포함 시 F1 0.759 vs 미포함 0.789)을 근거로, 다이제스트에 셀 서식·NFS를 추가하지 않는 현재 정책(활성 시트 layout만)을 excel_workbook_digest.py 모듈 주석에 출처와 함께 적어 재발을 막는다.",
          "다중 표 시트에 2단 관측(Chain of Spreadsheet) 도입 검토: used_range 안에 표가 2개 이상으로 감지되면 1단계에서 관련 표 영역만 고르고(규칙 우선, 애매하면 범용 모델), 2단계 플래너 프롬프트에는 그 영역의 다이제스트만 넣는다. excel_selection_context.py(사용자 선택 영역)와 결합하면 자연스러운 진입점이다.",
          "포맷 결론은 자체 모델로만 채택: HTML/LaTeX/Markdown 승자가 모델마다 다르다는 TabVerse·RealHiTBench 결과에 따라, 외부 벤치마크의 '최고 포맷'을 그대로 이식하지 말고 모든 다이제스트 포맷 변경은 A.X-4.0-Light 대상 planner_eval_v1.jsonl 154건 섀도 평가 또는 run_command_diagnostics로 게이트한다.",
          "좌표를 모델에게 시키지 않는 경계 유지: SUC 실측(GPT-3.5도 셀 주소 조회 39.7~44%)을 근거로, 상징 의도 JSON 스키마에 셀 좌표·범위 문자열 필드를 새로 추가하는 변경을 리뷰에서 거부한다 — 좌표 확정은 excel_param_binder.py 소관이라는 현 분업이 문헌으로 검증된 설계다."
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "조사"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "종합"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "조사:products",
      "phaseIndex": 1,
      "phaseTitle": "조사",
      "agentId": "a2528a26e43123da4",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976131957,
      "queuedAt": 1786976127096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976500460,
      "tokens": 50976,
      "toolCalls": 28,
      "durationMs": 367100,
      "resultPreview": "{\"findings\":[{\"claim\":\"Microsoft의 Semantic Interpreter(Office Copilot 계열 연구)는 LLM에게 자유 코드나 원시 API 호출이 아니라 ODSL(Office Domain Specific Language)이라는 제한된 고수준 DSL 프로그램을 생성시키고, 그 프로그램을 결정적 트랜스파일러가 실제 애플리케이션 API로 변환해 실행한다. 논문은 'LLM은 의도 이해에는 뛰어나지만 애플리케이션별 의도 수행에는 충분치 않다'고 명시하며, Analysis-Retrieval 방식으로 발화별 관련 DSL 문법·예제만 프롬프트에 골라 넣는다.\",\"source\":\"https://arxiv.org/abs/2306.03460\",\"relevance\":\"우리의 '상징 의도 JSO…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "조사:papers",
      "phaseIndex": 1,
      "phaseTitle": "조사",
      "agentId": "a1e5b684730644012",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976132235,
      "queuedAt": 1786976127096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976371606,
      "tokens": 38871,
      "toolCalls": 15,
      "durationMs": 238245,
      "resultPreview": "{\"findings\":[{\"claim\":\"SheetCopilot(NeurIPS 2023)은 원자 액션 집합 + 상태머신(observe-propose-revise-act) 루프로 GPT-3.5에서 Exec@1 87.3% / Pass@1 44.3%를 달성 — VBA 코드 직접 생성 베이스라인(77.8% / 16.3%) 대비 Pass@1 약 2.7배. Ablation에서 에러 피드백 단독이 Exec@1을 92.8%까지 올린 최대 단일 요인이고, 액션 문서+사용 예시 추가가 Exec@1 +17.2%p, Pass@1 +10%p.\",\"source\":\"https://arxiv.org/abs/2305.19308\",\"relevance\":\"우리 구조(고정 액션 어휘의 상징 의도 JSON + excel_step_repair/e…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "조사:constrained",
      "phaseIndex": 1,
      "phaseTitle": "조사",
      "agentId": "a88aeb0d1ecc4441f",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976132010,
      "queuedAt": 1786976127096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976546181,
      "tokens": 50905,
      "toolCalls": 31,
      "durationMs": 412820,
      "resultPreview": "{\"findings\":[{\"claim\":\"Ollama 0.5.0(2024-12-06)부터 format 필드에 JSON 스키마를 받아 출력을 문법 수준으로 제약하는 구조화 출력을 지원하며, 공식 블로그가 OpenAI 호환 엔드포인트(client.parse + response_format) 사용 예시를 직접 보여준다.\",\"source\":\"https://ollama.com/blog/structured-outputs\",\"relevance\":\"우리 LLM 경로는 Ollama OpenAI 호환 API(/v1/chat/completions) 단일 경로다. 별도 라이브러리(outlines 등) 도입 없이 이미 쓰는 API의 필드 하나로 제약 디코딩을 켤 수 있다.\"},{\"claim\":\"로컬 실측(이 머신, Ollama 0…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "조사:models",
      "phaseIndex": 1,
      "phaseTitle": "조사",
      "agentId": "a2b8e3e985f28ff88",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976131814,
      "queuedAt": 1786976127096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976450269,
      "tokens": 47828,
      "toolCalls": 20,
      "durationMs": 316909,
      "resultPreview": "{\"findings\":[{\"claim\":\"Qwen3 공식 기술보고서 기준 BFCL v3 점수는 4B 65.9/57.6, 8B 68.1/60.2, 14B 70.4/61.5 (thinking/non-thinking) — 7B급 오픈 모델 중 도구 호출 최상위권이다.\",\"source\":\"https://arxiv.org/pdf/2505.09388\",\"relevance\":\"우리 '상징 의도 JSON' 역할을 맡길 대체·비교 후보. 현재 A.X-4.0-Light와 같은 체급(4B/8B)에서 도구 호출이 사전학습부터 네이티브라, 프롬프트로 JSON을 강제할 때의 액션 발명 위험(한계 a)이 구조적으로 낮다. 단 thinking 모드 점수가 8점가량 높으므로 대화형 엑셀 편집의 지연과 트레이드오프.\"},{\"claim\":\"…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "조사:encoding",
      "phaseIndex": 1,
      "phaseTitle": "조사",
      "agentId": "aee4623344a65ca22",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976131776,
      "queuedAt": 1786976127096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976453307,
      "tokens": 47751,
      "toolCalls": 23,
      "durationMs": 321529,
      "resultPreview": "{\"findings\":[{\"claim\":\"SpreadsheetLLM(SheetCompressor)은 셀좌표+값 나열(바닐라 인코딩)이 토큰 폭발로 실패함을 보이고, 구조 앵커 추출(레이아웃 경계 ±4행/열만 유지, 내용 75% 제거·경계 97% 보존) + 역색인(값→주소범위 사전, 빈 셀 제외, 동일값 병합, 4.4×→14.9× 압축)으로 평균 25× 압축하면서 표 감지 F1을 GPT-4 인컨텍스트에서 25.6%p 올렸다.\",\"source\":\"https://arxiv.org/abs/2407.09025\",\"relevance\":\"우리 다이제스트(시트·사용범위·열=머리글·예시행 1개·저카디널리티 값 후보)가 문헌이 수렴한 '압축 요약' 방향과 일치함을 확인. 특히 '값→범위 역색인'은 우리 값 후보(categor…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "종합",
      "phaseIndex": 2,
      "phaseTitle": "종합",
      "agentId": "ab55ffac6c5d3a00b",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1786976550280,
      "queuedAt": 1786976548221,
      "attempt": 1,
      "promptPreview": "배경(실측 완료된 사실 — 재조사 불필요):\n- 로컬 전용 제품. Ollama로 7B급 모델 2개: 범용(A.X-4.0-Light, 한국어 인스트럭트), 플래너(ax7bplanner, 1000건 SFT).\n- SFT 플래너는 파라미터를 암기해 출력(=SUM(E:E)를 여섯 표현 모두에). 정확도 58~67%.\n- 범용 모델에게 \"상징 의도 JSON\"(task/range/열이름/옵션)만 시키면 96~100% — 이미 통합함.\n- 좌표·수식은 결정적 코드(바인더)가 워크북을 보고 조립. 규칙은 실행·안전 담당.\n- 남은 근본 한계: (a) 프롬프트로 JSON을 시키면 없는 액션 발명·형식 붕괴 가능, (b) 7B의 복잡 요청 이해, (c) 한국어.\n질문: 업계·학계는 이 문제를 어떻게 풀었고, 우리가 지금 채택…",
      "lastProgressAt": 1786976692239,
      "tokens": 45583,
      "toolCalls": 0,
      "durationMs": 141959,
      "resultPreview": "# 근본 해결책 로드맵 — 조사 5각도 종합\n\n전제: 상징 의도 JSON + 범용 모델(96~100%), 결정적 바인더, 154건 평가 배터리는 **이미 구현·실측 완료**이므로 아래에서 다시 제안하지 않는다. 이 로드맵은 남은 근본 한계 (a) 없는 액션 발명·형식 붕괴, (b) 7B의 복잡 요청 이해, (c) 한국어만 다룬다. 모든 항목은 §2 절차(before/after 실측 + 실행 id + 개발일지)로 게이트한다.\n\n핵심 확인: Microsoft Semantic Interpreter(ODSL+결정적 트랜스파일러, arxiv 2306.03460), SheetCopilot(액션 API가 자유 코드 생성을 Pass@1 2.7배로 이김, arxiv 2305.19308), TableGPT2(표 특화 7B는 …"
    }
  ],
  "totalTokens": 281914,
  "totalToolCalls": 117
}
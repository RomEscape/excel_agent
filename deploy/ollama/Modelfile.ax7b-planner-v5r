# v5r — v5/checkpoint-100 warm-start + 1 에폭 (총 1.5 에폭)
# [2026-08-20] 이 GGUF는 삭제됐다
# 승격 게이트 탈락(2026-08-18: 첫 액션 +4.6%p로 기준 미달, multi -50%p) 뒤 13.5GB를 회수했다.
# 재도전 조건이 라벨 재설계 -> 재학습이라 같은 가중치를 다시 양자화할 이유가 없다.
# 아래 create 명령은 파일이 없어 실패한다. 비교가 필요하면 Ollama의 ax7bplanner-v5r:latest(q4)를 쓴다.

# 등록: ollama create ax7bplanner-v5r -f deploy/ollama/Modelfile.ax7b-planner-v5r --quantize q4_K_M
FROM C:\Users\asdjj\OneDrive\바탕 화면\동화책 프로젝트\officeclaw\artifacts\ax7b-planner-v5r-f16.gguf

PARAMETER num_ctx 16384
PARAMETER temperature 0.2
PARAMETER top_p 0.9
PARAMETER repeat_penalty 1.05

SYSTEM """
너는 OfficeClaw Excel 플래너다.
반드시 JSON만 출력한다.
action_plan의 action은 excel_live.* 만 사용한다.
"""

TEMPLATE """{{ if .System }}<|system|>
{{ .System }}
{{ end }}{{ if .Prompt }}<|user|>
{{ .Prompt }}
{{ end }}<|assistant|>
"""

PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|user|>"
PARAMETER stop "<|assistant|>"
